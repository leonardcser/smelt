# P12 — Lua-side custom paint regions

**Status:** drafted **Drafted:** 2026-05-07

P12 closes the last surface gap from the P11 Lua-API audit: option G —
custom paint regions accessible from Lua. The infrastructure already
exists (`smelt-term::render_with_paints` walks `LayoutTree::Leaf(PaintId)`
nodes and dispatches to a host-supplied paint function); P12 wires that
dispatch to Lua-registered painters.

The driving signal: a 1500-line widget like a snake game, a sparkline
panel, or a half-block treemap doesn't belong in the Buffer model
(lines + highlight extmarks). Its content is *cells*, not *text*. The
Buffer pipeline (text shaping → highlight projection → cell write) is
the wrong abstraction; what these widgets want is a `GridSlice` they
can write directly.

tcloc demonstrates the pattern from Rust today (`render_legend` /
`render_treemap` write straight into `GridSlice`); P12 is plumbing the
same dispatch from Lua.

## Decisions made

- **Paint dispatch is per-PaintId**, registered on `Ui` (or the host's
  Surface for non-editor consumers). The renderer's
  `render_with_paints` already walks paint ids; P12 adds a Lua-side
  registry keyed by `PaintId`, stored on the editor `Ui`'s callback
  table, that wraps the painter as a `Callback::Lua(handle)`.
- **`GridSlice` exposed as Lua userdata**, not a copy-and-commit
  buffer. The painter writes directly into the renderer's grid for the
  leaf's rect — same semantics as a Rust-side painter. No allocation
  per frame.
- **Bulk primitives by default; per-cell as escape hatch.** API: `set`,
  `put_str`, `fill_rect`, `width`, `height`. The first three cover 99%
  of widgets at one FFI call per drawn shape, not per cell. A snake
  game with 30-cell body and 1 food becomes ~3 calls/frame regardless
  of grid size.
- **No frame-buffer upload primitive in Phase A.** `slice:blit(fb)`
  is the right shape if FFI per-shape becomes the bottleneck, but
  profiling-driven. Snake at 60fps is fine on the direct path; charts
  and sparklines run at update-rate, not framerate. Add only if a real
  widget shows the cost.
- **Reuse `parse_style` / `parse_color` from `lua/ui_ops.rs`.** The
  Phase 1 Lua-API work already landed a Lua → `Style` parser. Style
  args on paint primitives use the same shape (`{ fg = "red", bold = true }`)
  for surface consistency.
- **Painter registration returns a `paint_id: u64`** usable in
  `LayoutTree.leaf(paint_id)` *and* directly as the leaf of an
  overlay. No new layout-tree node type — paint ids are just
  `PaintId`s outside the WinId-allocated range.
- **Painter lifecycle is host-managed.** The host calls
  `smelt.paint.unregister(id)` when the panel closes; ids leak
  otherwise but that's a Lua bug, not a renderer bug. (Mirrors today's
  `smelt.win.close` discipline.)
- **Skipped:** off-screen composition surfaces, GPU-style retained
  scenes, sub-cell shading helpers (callers compose Unicode block
  glyphs themselves). All viable later if a widget demands them; none
  enable the snake-game-class motivation.

## Architecture

Today, `smelt-edit::Ui::render` walks the active layout tree and
overlays. For every `LayoutTree::Leaf(PaintId)`, it consults a
dispatcher: WinId-range ids route to `Window::render` (the Buffer
painter); other ids go to a host-supplied paint function. The renderer
itself ascribes no semantics to the id — that's why tcloc, with no
windows at all, can use the same primitive.

P12 adds a third dispatcher branch: ids registered with `smelt.paint`
route to a Lua callback. The Lua callback receives a `GridSlice`
userdata sized to the leaf's resolved rect, runs to completion, and
returns nothing (no result needed — writes happened in place).

```
LayoutTree::Leaf(paint_id)
        │
        ▼
  paint dispatcher
        │
        ├── PaintId in WinId range  →  Window::render(slice, theme, …)
        ├── Lua-registered          →  Lua: paint_fn(slice)            ← P12
        └── otherwise                →  host's custom painter
```

The paint id allocation lives on `Ui` (and on `Surface` for non-editor
consumers — `Surface::register_paint` in P11.D).

## Phase A — paint registry + GridSlice userdata (one PR, ~250 LOC)

Self-contained. After this lands, a Lua plugin can paint a snake game
or a sparkline directly into a leaf rect.

### A.1 — paint id allocator on `Ui`

**What:** `Ui::register_paint(callback) -> PaintId` allocates a fresh
id outside the WinId range, stores `(id, Callback::Lua(handle))` on a
`paint_callbacks: HashMap<PaintId, Callback>` table. `Ui::unregister_paint(id)`
removes it. The renderer's paint dispatcher consults this table for
ids it doesn't recognise as windows.

**Algorithm:** allocator starts at `1 << 32` (well above any WinId);
monotonic counter; rolls over after `u64::MAX` (won't happen). The
dispatcher's existing `if WinId(id) maps to Window` branch falls
through to `paint_callbacks.get(&PaintId(id))`; matched callbacks fire
with the paint slice.

**Files:**

- `crates/edit/src/lib.rs`: `paint_callbacks` field on `Ui`,
  `register_paint` / `unregister_paint` methods, dispatcher branch in
  `render_with_paints`.
- `crates/term/src/surface.rs`: matching `Surface::register_paint` for
  non-editor consumers (P11.D follow-up — same shape).
- New test in `crates/edit/src/lib.rs`: register a paint id, attach
  it to a layout, render to a fixture grid, assert the painter was
  called with the expected rect.

**Done when:** a Rust test registers a paint callback that fills its
slice with `'X'`, builds a layout with that leaf, renders, and the
grid shows `'X'` in exactly the leaf's rect.

### A.2 — `GridSlice` Lua userdata

**What:** wrap `&mut GridSlice<'_>` as Lua userdata for the duration
of one paint call. Methods:

- `slice:set(row, col, char, style)` — single cell.
- `slice:put_str(row, col, text, style)` — bulk row write, clipped.
- `slice:fill_rect(row, col, w, h, char, style)` — block fill.
- `slice:width()`, `slice:height()` — leaf rect dimensions.

`style` is a Lua table parsed by the existing `parse_style` from
`lua/ui_ops.rs` (shared parser → no surface drift between titles,
extmarks, and paint).

**Lifetime hazard:** `GridSlice<'_>` borrows the renderer's grid
mutably. The userdata can't outlive the paint call. mlua handles this
via scoped userdata (`Lua::scope`): the userdata is invalidated at
scope exit, and any Lua reference held past the paint call errors on
next use. Same pattern smelt already uses for `BufferRef` /
`WindowRef`.

**Files:**

- `crates/tui/src/lua/api/paint.rs` (new): the userdata + methods.
- `crates/tui/src/lua/api/mod.rs`: register the new module.
- `crates/tui/src/lua/ui_ops.rs`: re-export `parse_style` /
  `parse_color` so the paint module reuses them. (Currently
  pub(crate); flip to pub(super) and add a `lua` module re-export.)
- `crates/tui/tests/lua/paint.rs` (new): integration test driving a
  Lua painter that writes a known pattern, asserting the grid.

**Done when:** a Lua plugin can register a painter, call all four
primitives, and have the output appear in the rendered grid as
expected. Out-of-bounds writes clip cleanly. Style parsing accepts
the same surface as titles.

### A.3 — `smelt.paint` Lua module

**What:** thin Lua wrapper that surfaces the registry:

```lua
smelt.paint.register(fn) -> paint_id
smelt.paint.unregister(paint_id)
```

`fn` receives the slice userdata once per frame the leaf is visible.
The id can be used as the `win` of an overlay item, or as a
`LayoutTree.leaf(...)` argument anywhere a `WinId` is accepted today.

**Files:**

- `runtime/lua/smelt/paint.lua` (new): wrapper module exposing
  `register` / `unregister`.
- `runtime/lua/smelt/init.lua`: hoist it to `smelt.paint`.

**Done when:** `:lua smelt.paint.register(function(s) s:fill_rect(0,
0, s:width(), s:height(), 'X', { fg = 'red' }) end)` followed by
attaching the returned id to an overlay produces a red `X` field.

### A.4 — overlay item accepts paint id

**What:** today, `overlay item.win` reads a `WinId` and asserts
`app.ui.win(win).is_some()`. Extend it to accept a paint id (any
`PaintId` outside the WinId range, registered via `smelt.paint`).

**Files:**

- `crates/tui/src/lua/ui_ops.rs:60-65` (`open_overlay` items loop):
  resolve the id against both `app.ui.win` and `app.ui.paint_callbacks`;
  error on neither. Build `LayoutTree::leaf(PaintId(raw_id))` instead
  of `LayoutTree::leaf(WinId(raw_id))`.

**Done when:** an overlay with `items = { { win = paint_id, … } }`
opens, the painter fires per frame for that leaf's rect, and the
existing window-backed overlay path is unchanged.

## Phase B — example plugin + storybook (one PR, ~150 LOC)

Stress-tests the API with a real-shape widget. Optional in the sense
that it doesn't gate further work, but it's the dogfood pass that
catches surface defects.

### B.1 — sparkline plugin

**What:** `runtime/lua/smelt/plugins/sparkline.lua` — reads a sample
buffer (last N values), renders a half-block sparkline into a paint
region. Exposes `:Sparkline open` / `:Sparkline push <n>`. Pure Lua,
~80 lines.

**Why this widget:** half-block rendering (`▁▂▃▄▅▆▇█`) hits sub-cell
expression that's awkward in Buffer (one cell = one glyph + style; no
way to express "this cell is 5/8 filled" except via the glyph itself,
which `put_str` handles fine). Validates the slice API on a plausible
real-world shape.

**Files:**

- `runtime/lua/smelt/plugins/sparkline.lua` (new).
- Wire-up in `runtime/lua/smelt/init.lua`.

**Done when:** `:Sparkline open` overlays a 40×8 panel, `:Sparkline
push 0.5` advances the series, the sparkline updates without flicker.

### B.2 — storybook story

**What:** add a paint-region story to the storybook tests so the
renderer-level path stays covered. Story: a 20×10 paint region writes
a checkerboard via `fill_rect` calls; snapshot test asserts the grid.

**Files:**

- `crates/tui/tests/storybook/stories/paint.rs` (new).
- `crates/tui/tests/storybook/snapshots/`: generated snapshot.

**Done when:** the storybook run produces a stable snapshot for the
paint region; story renders identically across runs.

## Phase C — frame-buffer escape hatch (deferred; gated on profiling)

Only worth building if a real widget shows that per-shape FFI is the
bottleneck. The shape:

```lua
local fb = smelt.framebuf.new(w, h)  -- userdata, owns a Vec<Cell>
fb:set(row, col, char, style)         -- pure Lua → C cell write
slice:blit(fb, dst_row, dst_col)      -- one FFI: bulk memcpy
```

Skip until a widget needs it.

## Files (cross-phase summary)

New:

- `crates/tui/src/lua/api/paint.rs` — GridSlice userdata + methods.
- `runtime/lua/smelt/paint.lua` — Lua module wrapper.
- `runtime/lua/smelt/plugins/sparkline.lua` — example plugin (B).
- `crates/tui/tests/storybook/stories/paint.rs` — storybook story (B).
- `crates/tui/tests/lua/paint.rs` — integration test.

Modified:

- `crates/edit/src/lib.rs` — paint registry + dispatcher branch.
- `crates/term/src/surface.rs` — matching surface API for non-editor
  consumers.
- `crates/tui/src/lua/ui_ops.rs` — accept paint ids in overlay items;
  expose `parse_style` / `parse_color` for the paint module.
- `crates/tui/src/lua/api/mod.rs` — register the new paint module.
- `runtime/lua/smelt/init.lua` — hoist `smelt.paint`.

## Out of scope

- Off-screen composition (render to a texture, blend, etc.). Adds
  retained-mode complexity for no current widget.
- GPU acceleration / dirty rects within a paint region. Compositor
  already does cell-level diffing on the final grid; widget-level
  dirty tracking belongs in Lua if a plugin wants it.
- Mouse hit-testing on paint regions. Already supported via
  `HitTarget<P>` from P11; P12 doesn't change this — paint regions
  participate in hit-testing as opaque rects, and Lua receives mouse
  events via the existing keymap/event path.
