# P6 — Streaming + lifecycle polish

**Status:** done **Started:** 2026-05-02 **Landed:** 2026-05-02

Lock down the engine→buffer streaming path and the lifecycle gates
(confirms gate, cooperative cancel, dialog stacking).

Sub-phases:

- **P6.a** — Streaming pipeline hardening. ✅ Completed.
  Verify `EngineEvent::TextDelta` → `EngineClient` → `Buffer::append`
  never drops or reorders chunks. Added 18 `StreamParser` unit tests
  covering chunk boundary independence, structural boundary detection
  (code blocks, tables, paragraph breaks), thinking→text transition,
  tool/exec lifecycle, and the no-drop invariant (char-by-char feed).
  Buffer's `on_block` callback is still gated on P1.a-tail (`Buffer::attach`).
- **P6.b** — Confirms gate. ✅ Completed.
  `EngineClient::recv` returns `pending()` and `try_recv` returns
  `Err(Empty)` while `Confirms::is_clear()` is false. The two outer
  guards in `mod.rs` (`select!` branch + drain-loop `if`) are removed.
  `focused_overlay_blocks_agent()` is dropped from `dispatch_control`
  in `agent.rs` — the single `Confirms` gate is the only engine-drain
  mechanism. `Host::confirms()` retires (all callers reach
  `core.confirms` directly).
- **P6.c** — Cooperative cancel. ✅ Completed.
  Each `LuaTask` carries a `tokio_util::sync::CancellationToken`.
  `cancel_agent()` calls `LuaRuntime::cancel_tasks()` which cancels every
  token and flips parked `Sleep`/`External` waits to `Ready` with a
  `{"__cancelled"=true}` marker. `step_task` also prevents newly-yielded
  tasks from parking when the token is already cancelled. The Lua
  bootstrap (`_bootstrap.lua`) checks for `__cancelled` in `sleep`,
  `task.wait`, and `tools.call` resumed values and calls
  `error("cancelled")` so normal Lua `pcall` handles cleanup.
  `run_streaming` reads the current task's token via TLS and pushes a
  cancel marker instead of the real result when aborted.
- **P6.d** ✅ — Dialog stacking + Esc chain. `Ui::dispatch_key` now
  routes bare Esc to the focused window's keymap first; only if the
  keymap returns `Ignored` does the modal dismiss fire. `Ui::overlays`
  z-stack already enforces open-order = z-order and modal-on-top blocks
  focus to lower overlays.
- **P6.e** ✅ — Cursor shape global. Already landed in P1.f.5;
  `Ui::cursor_shape` is the single source of truth and the render loop
  updates it per frame based on focus ownership.
- **P6.f** ✅ — Cell-event fan-out tests. Added
  `builtin_cells_queue_subscribers_on_set` covering all 21 built-in cells
  (stateful + event-shaped) asserting that `set_dyn` queues subscribers
  for every name. Existing projector tests (`event_payload_projectors_*`,
  `token_usage_projector_*`) already verify the Lua-side payload shape.

## Decisions made

- **P6.d — Esc chain reorder: focused window first, modal dismiss second**
  (`<this session>`) — `Ui::dispatch_key` previously intercepted bare Esc
  before the focused window's keymap had a chance to consume it. The
  reorder lets a leaf (e.g. an input panel clearing selection on Esc)
  prevent modal dismissal. The existing
  `modal_esc_fires_dismiss_once_on_overlay_root` test still passes because
  the leaf has no Esc keymap; a new test
  `modal_esc_consumed_by_focused_leaf_does_not_dismiss` verifies the
  consumption path.
- **P6.f — Subscriber coverage as `PendingFire` queue assertions**
  (`<this session>`) — rather than driving the full Lua callback drain
  (which requires an `mlua::Lua` runtime and `LuaHandle` registry keys),
  the test asserts that `set_dyn` on each built-in cell produces a
  `PendingFire` with the right name and typed payload. The callback
  execution path is already covered by `subscribe_queues_fire_on_set`.
- **P6.a — `parse_marker` trace tests as `StreamParser` unit tests**
  (`<this session>`) — rather than adding an intrusive trace vector to
  `StreamParser`, the tests treat `BlockHistory` as the trace surface:
  feed deltas, assert the exact sequence of `Block` variants and
  `Status::Streaming` → `Status::Done` transitions. This documents
  current behavior without modifying the hot path. `Block` gained
  `Debug + PartialEq` derives to enable direct equality assertions.
  One known limitation is documented: code fences split *mid-line*
  across chunks are not detected as fences because `StreamParser`
  syncs per-chunk and fences are recognized at line boundaries.
- **P6.b — `Arc<AtomicBool>` shared flag for confirms gate**
  (`<this session>`) — `Confirms` owns an `Arc<AtomicBool>` that
  `register`/`take` update; `EngineClient` clones it at construction
  and reads it in `recv`/`try_recv`. This avoids a self-referential
  struct in `Core` (both `confirms` and `engine` are fields of the
  same struct). The `Host` trait drops `confirms()` since all
  consumers already access `core.confirms` directly.
- **P6.c — Cooperative cancel via `CancellationToken` + TLS + Lua
  bootstrap throw**
  (`<this session>`) — Rust side: `tokio_util::sync::CancellationToken`
  per `LuaTask`; thread-local `CURRENT_TASK_CANCEL` set around
  `thread.resume` so async bindings (e.g. `run_streaming`) can pass
  the token to spawned tokio tasks. Lua side: the three yield
  primitives (`sleep`, `task.wait`, `tools.call`) check for
  `__cancelled` and call `error()`, making cancellation catchable by
  `pcall`. Three unit tests cover: cancelled sleep, cancelled
  external, and a running task that yields after cancel.

## Deferrals

(none yet)

## Open questions

(none yet)

## Next phase entry conditions

P7 entry: all streaming paths are trace-verified; confirms gate is the
single engine-drain mechanism; cancel propagates through the full Lua→Rust
stack; dialog stacking has no special framework code; every built-in cell
has a subscriber test.
