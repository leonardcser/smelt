# P7 — Finalize

**Status:** done **Started:** 2026-05-02 **Landed:** 2026-05-02

## Decisions made

- `permissions::with_overrides` is test-only; gated with `#[cfg(test)]` rather
  than `#[allow(dead_code)]`.
- `LuaShared` contains genuine Lua-runtime state (handle registries, atomic
  counters, coroutine runtime, deferred queues, config registries). No mirror
  fields remain to sweep.

## Deferrals

- Human parity walk (triple-click yank, drag-extend, esc chain, selection bg,
  vim modes, cmdline, picker, confirm dialog, notification toast, statusline
  live update, theme switch) is out of scope for automated completion; it is
  documented as a recommended manual step in `REFACTOR.md`.

## Open questions

(none)
