# P9 — Make the architecture true

**Status:** in-progress **Started:** 2026-05-04 **Landed:** —

Close the deferral chain (`P1.a-tail → P4.b → old P9.b`) and purge
places where Rust still encodes Lua-shaped decisions. Hygiene + parity
walk live in P10.

## Sub-phase status

| ID    | Topic                                                              | Status        |
| ----- | ------------------------------------------------------------------ | ------------- |
| P9.a  | Well-known win/buf ids → `app/well_known.rs`                       | ✅            |
| P9.b  | `Buffer` + `BufferParser` + `Style` + `UndoHistory` to `core`      | ✅            |
| P9.c  | Transcript pipeline as `BufferParser` impls; copy unification      | 🚧 partial    |
| P9.d  | Tool name matches deleted from shared Rust                         | 🚧 partial    |
| P9.e  | HlGroup-id model (extmarks carry interned ids)                     | ✅            |
| P9.f  | Unified extmark keyset + `Rect` to `geometry.rs`                   | ✅            |
| P9.g  | Plugin auto-discovery + project-local trust gate                   | ✅            |
| P9.h  | Cell `(new, old)` payload                                          | ✅            |
| P9.i  | Provider middleware                                                | ⏸ deferred   |
| P9.j  | Loader override search path                                        | ✅            |
| P9.k  | Concurrent tool execution (`FuturesUnordered`)                     | ✅            |
| P9.l  | Embedded Lua tree via `include_dir!`                               | ✅            |
| P9.m  | Single LuaRuntime instance                                         | ✅            |
| P9.n  | Vim becomes Lua-extensible                                         | 📝 sketched   |
| P9.o  | Prompt is a Window                                                 | 📝 sketched   |
| P9.x  | Config binding fidelity (provider models / MCP / settings)         | 📝 sketched   |

## Strategic concerns (audit 2026-05-05)

Plan-vs-code drift surfaced from a code audit; resolutions below.

### Tool callback proliferation

`bash.lua` registers 11 callbacks. The plan promised one `hooks` fold
of `needs_confirm + approval_patterns + preflight`; reality kept all
three plus `decide`. Sort by kind:

- **Render callbacks legitimately plural.** `render` /
  `render_summary` / `render_subhead` / `header_suffix` / `preview`
  paint into different Buffer slots at different lifecycle points.
  Folding to one `render(slot, …)` would just dispatch on slot
  internally; clarity loses. Drop the plan promise; keep the shape.
- **Decision callbacks redundantly plural.** `decide` /
  `needs_confirm` / `approval_patterns` answer one question
  ("allow / ask / deny + which patterns to remember"). The
  original P9.d promise stands: collapse to one
  `decide(args, mode, ctx) -> { decision, approval_patterns?,
  confirm_message? }`. Carries to P9.d closeout.

### "No defer on size" applied unevenly

Spirit was: don't procrastinate on architectural improvements.
P9.b/d/e/f/l all landed despite being heavy. P9.i (provider
middleware, ~500 LOC) sits open with no consumer — speculative
seam. **Restate: defer for absent consumer, not for size.** P9.i
moves to deferrals; P9.c open work that the entry conditions
already require stays open (don't call required work "deferred").

### P9.c is open work, not "follow-ups deferred"

P9.b's log called three pieces "clean follow-up sub-phases, not
blockers":

- per-`Block` `BufferParser` impls under
  `tui::content::transcript_parsers/`
- prompt rendering through the same parser
- one `copy_range(buf, range)` primitive

But the entry conditions for P10 demand all three.
`transcript_present/mod.rs` is 1330 LOC (grew from 1153);
`Window::mouse_yank_text` (`window.rs:801`) is still a separate
copy path. Resurrect P9.c as a live sub-phase.

### Cell typing vs settings typing asymmetry

Built-in cell name typing was deferred ("typo-as-no-op rare;
runtime checks already reject undeclared names"). Settings has no
equivalent guard — `smelt.settings.set("vimode", true)` silently
no-ops. P9.x adds reject-unknown to settings. Cells stay deferred
(already validated). The asymmetry was the bug, not the deferral.

## Open sub-phase sketches

### P9.c — Transcript pipeline as `BufferParser` impls

Three pieces, land together:

1. **Per-`Block`-variant parsers** under
   `tui::content::transcript_parsers/`. Each block (Text / Code /
   Tool / Image / …) becomes a `BufferParser` impl writing into
   the per-block Buffer. The central `layout_block_into` switch in
   `transcript_present/mod.rs` deletes; arms become parser
   `on_attach` + parser-fed `on_block` paths.
2. **Prompt rendering through the same parser.** Prompt today has
   its own layout path (`prompt_buf.rs`); content shape matches a
   user-message `Block::Text`. Unify.
3. **One `copy_range(buf, range)` primitive.** `copy_range` exists
   in `transcript_snapshot.rs:61` for transcript-mouse selection;
   `Window::mouse_yank_text` is parallel. Fold the second onto the
   first; vim / kill-ring read through the same primitive.

Closes the P9.b "follow-ups" + clears entry conditions for P10.

### P9.d — Closeout

Five remaining hardcoded sites + the decide-callback collapse.

Hardcoded:

- `core::permissions::approvals.rs:149` — `is_auto_approved` bash
  branch. Generalize to "tool registers extra-ruleset
  consultation."
- `core::permissions::rules.rs:25-29, 158-159` — `RawModePerms`
  fields + iteration order. Change to
  `tools: HashMap<String, RawRuleSet>` with one well-known generic
  bucket; tools register subcommand buckets.
- `core::custom_commands.rs:27-28` — `RuleOverride` carries `bash`
  + `web_fetch` fields. Generalize.
- `tui::lua::api::engine.rs:170-171` — `CommandOverrides` parser
  hardcoded buckets.
- `tui::lua::api::permissions.rs:44, 51` — `set_rules` parser
  hardcoded buckets.

Collapse `decide` + `needs_confirm` + `approval_patterns` Lua
callbacks into one returning-decision callback.

Verification grep:

```
grep -nE 'name *== *"(bash|read_file|write_file|edit_file|glob|grep|web_fetch|web_search|notebook_edit|exit_plan_mode|load_skill|ask_user_question)"' \
  crates/{core,engine,tui}/src/
```

returns zero hits.

### P9.n — Vim becomes Lua-extensible

`vim.rs` keymap dispatch is hardcoded Rust; users can't override
`dd` in init.lua even though every other interactive surface is
recipe-driven.

- **P1.d.5d** — registers / dot-repeat / undo move from
  `VimWindowState` to `Buffer` (per-buffer state lives where
  Buffer does).
- **P1.d.5f.2d** — vim dispatch table flattens onto the Lua keymap
  registry; `vim.rs` becomes the recipe of default keymap entries.

Framing is **Lua extensibility parity**, not "vim.rs is big" —
file size shrinks as a side effect. Lands after P9.f.

### P9.o — Prompt is a Window

`tui::input::PromptState` (~1170 LOC) is the only interactive
surface that isn't a Window over a Buffer with a keymap recipe.

- Edit state → Buffer.
- Cursor / scroll / selection → Window.
- Keymap → Lua via `widgets/input.lua`.
- Completer → ghost-text extmark + Overlay picker.
- Attachments stay as a small input-side concern.

Lands after P9.n (rides the same Lua keymap dispatch path).

### P9.x — Config binding fidelity

Mechanical reject-unknown / fidelity bugs in config-time bindings.
Plugin-ergo win, ~80 LOC total.

- **`provider.rs`** — `ModelConfig` carries 10 per-model fields
  (temperature / top_p / top_k / min_p / repeat_penalty /
  tool_calling / pricing). The Lua binding only reads the model
  name; every other field drops on the floor. Accept models as
  string or table.
- **`mcp.rs`** — register silently ignores `type` / `timeout` /
  `enabled`. Read them; reject unknown `type` so a future remote
  variant doesn't change config meaning silently.
- **`settings.rs`** — collapse `set` / `toggle` / `snapshot` into
  field access via metatable. `smelt.settings.vim = true` /
  `local v = smelt.settings.vim` / `for k, v in pairs(smelt.settings)`
  with `__newindex` / `__index` / `__pairs` routing to the same Rust
  paths today's three functions reach. Unknown keys error at the
  access site, not silently. Field names match `ResolvedSettings`
  (`vim`, `show_prediction`, `show_slug`) — today's `set` /
  `toggle` / `snapshot` use three different vocabularies for the
  same booleans; this collapses them to one. Drop `let _ =
  settings.apply(key, value)` in `runtime.rs:230` (silently
  discards validation errors).

### P9.i — Provider middleware (deferred)

Neutral `ProviderRequest` / `ProviderResponse` between
`EngineClient` and per-kind serializers; `Vec<Box<dyn ProviderMiddleware>>`
with return-payload semantics. ~500 LOC.

**Deferred** — no concrete consumer. Land when first plugin needs
redaction / A/B swap / cassette capture; sketch stays here.

## Decisions

Ordered by surprise value, not chronology.

- **No defer on size; defer on absent consumer.** Refines the
  earlier "no defer on size" rule. Speculative seams without
  callers wait; required architectural improvements don't.
- **Render callbacks plural by design; decision callbacks plural
  by accident.** Render slots have legitimate signature differences
  per Buffer slot. Decision callbacks duplicate one yes/no/ask
  answer.
- **`is_auto_approved` bash branch was acknowledged residual.**
  Closing it requires generalizing "tool's runtime approval
  consults an extra ruleset" without re-introducing a name match.
- **Trust UX is `/trust` command, not startup dialog.** Interactive
  startup prompts couple loading to the event loop; deny-by-default
  + toast + opt-in slash command keeps loading sync.
- **Loader order: embedded → user init → global plugins → project
  plugins+init.** User init can override embedded defaults; project
  config is last so per-repo `<cwd>/.smelt/init.lua` always wins.
  Project content gated by SHA-256 hash + `/trust` mark.
- **Single-LuaRuntime autoload runs without TLS app pointer.**
  Verified safe — every embedded autoload module's top-level code
  is either a registration call (`smelt.{tools,cmd,au,cell}.register`,
  writes to `LuaShared`) or a pure data lookup. Anything reading
  live app sits inside callbacks fired after `start()`.
- **Anonymous HlGroup interning by content hash.** Theme switches
  no longer rewrite buffers — they mutate `Theme.styles[id]` once.
  Sites that haven't picked a name yet content-hash their `Style`
  into `__anon__/<hex>` keys.
- **Cell `(new, old)` via `mem::replace`.** `Cells::set_dyn` swaps
  in-place, stashes prior `Rc<dyn Any>` on `PendingFire`. Direct
  subscribers fire `(new, old)`; glob subscribers `(name, new, old)`.
- **`include_dir!` collapse for embedded Lua.** Side effect:
  three plugins embedded but never `require`'d (`plan_mode`,
  `predict`, `yank_block`) now autoload — fixed a latent bug
  where the `exit_plan_mode` tool wasn't installed.
- **No big-file decomp for size's sake.** `agent.rs` (1948),
  `ui/mod.rs` (~1600), `window.rs` (1198) stay as-is — single
  coherent structs, splitting fragments state.

## Deferrals

- **P9.i** (provider middleware) — no consumer. Land on first ask.
- **EmmyLua stubs** (`_meta/smelt.lua`) — post-P10 polish.
- **Plugin handle / lifecycle** (`smelt.plugin.define{...}` +
  teardown) — over-engineering for static-file plugin set. Revisit
  when an ecosystem needs hot reload or `/plugins disable`.
- **Reactive statusline binding** (`{ bind = cell, fmt = fn }`).
  Composer ticks once per main-loop iteration. Profile says don't
  bother yet.
- **Plugin manifest blocks** — no `/plugins` UI consumer.
- **Multi-mode `keymap.set({"n","v"}, ...)` sugar** — trivially
  composed in Lua.
- **`vim.ui.set_picker`-style override hook** — no second picker
  exists.
- **Built-in cell name typing** — `set_dyn` already rejects
  undeclared names; protection exists.
- **`smelt.cmd.read_md` bridge** — no consumer asked; .md and Lua
  command systems coexist fine.

## Entry conditions for P10

- Transcript renders through `BufferParser` impls in
  `tui::content::transcript_parsers/`; `transcript_present/`
  deleted.
- One `copy_range(buf, range)` primitive replaces every divergent
  copy path.
- Verification grep (P9.d closeout) returns zero hits.
- `decide` + `needs_confirm` + `approval_patterns` Lua callbacks
  collapsed to one returning-decision callback.
- Vim keymap dispatch is recipe-style over the Lua keymap registry;
  per-buffer registers / dot-repeat / undo live on `Buffer`.
- Prompt input is a Window over a Buffer with a Lua keymap recipe;
  `PromptState` god-struct dissolved.
- Provider / MCP / settings bindings reject unknown keys; per-model
  fields land in `ProviderConfig::models`.
- `cargo fmt && cargo clippy --workspace --all-targets -- -D warnings && cargo nextest run --workspace`
  green.
- `refactor/check.sh` green.
