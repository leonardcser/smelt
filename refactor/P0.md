# P0 — Clear the deck

**Status:** in-progress
**Started:** 2026-04-29
**Landed:** _(empty while in-progress)_

## What shipped vs what was planned

Tracked here as each deletion lands. The plan is the bullet list under
`REFACTOR.md` § P0.

- [ ] `crates/ui/src/buffer_view.rs` + every `BufferView::new(...)` call site.
- [ ] `crates/ui/src/component.rs` (`Component` trait, `WidgetEvent`).
- [ ] `PanelWidget` trait + the panel-widget multiplexing inside
  `crates/ui/src/dialog.rs`.
- [ ] 6-variant `Placement` enum + `add_layer` / `register_split` /
  `set_layer_rect` / `focus_layer` stringly-typed layer plumbing.
- [ ] `crates/tui/src/theme.rs` constants module.
- [x] Per-widget `selection_style` fields on `TextInput`,
  `NotificationStyle`, `DialogConfig`, `DrawContext`, `Compositor`, plus
  `Ui::set_selection_bg` / `Ui::selection_style()` shim.
- [x] `Ui::handle_mouse_with_lua` / `Ui::handle_mouse_for` /
  `classify_widget_action`.
- [x] `MouseAction::Yank(String)` and `WidgetEvent::Yank(String)`.
- [x] `crates/ui/src/buffer_list.rs`.

End state: `ui` doesn't compile. `tui` doesn't compile. The bones of the
target are visible because the noise is gone.

## Decisions made while coding

_(filled in as decisions surface)_

## Files / types / functions changed

_(filled in as deletions land)_

## Deferrals

_(empty until something slips)_

## Verification

- Tree status at end of phase: red (expected).
- Tests: `cargo nextest run --test scenarios` cannot run while `tui`
  doesn't compile. Re-run at the P1 boundary; the 5 baseline snapshots
  are the regression gate for the engine→JSONL path.
- `refactor/check.sh`: must stay green throughout — it checks docs,
  not code.

## Next phase entry conditions

P1 starts with `ui` deliberately empty of the legacy primitives. P0 is
done when no symbol from the bullet list above remains in the tree.
