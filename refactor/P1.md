# P1 — UI primitives (the load-bearing phase)

**Status:** in-progress
**Started:** 2026-04-29
**Landed:** (in progress — P1.0, P1.a foundation, and P1.b complete; P1.c..P1.f remain)

P1 is the long phase. It rebuilds the `ui` crate around the target
primitives (`Buffer`, `LayoutTree`, `Overlay`, `Window`, `Theme`,
`UiHost`, `Ui` facade) and absorbs the structural deletions that P0
deferred. Sub-phases land independently, each with the tree green at
the boundary; this log accumulates entries as they close.

## What shipped vs what was planned

### P1.0 — Theme registry + paired structural deletions ✅

Landed: `decb0ab` → `e489a79` (12 commits + status updates).

Planned: nvim-style highlight-group registry (`ui::Theme { groups,
links }`) replacing the host-side `crate::theme::*` atomics; pair
the deletion of `crates/tui/src/theme.rs` constants with the
registry's introduction.

Shipped exactly that. `ui::Theme` lives on `Ui`, threads through
`DrawContext`, and is populated each frame from `theme.is_light()`
+ `theme.accent()` + `theme.slug()`. Renderers read `ctx.theme.get(group)`
instead of host constants. The full atomic set on `tui::theme` (BG,
ACCENT, SLUG) collapsed onto `ui::Theme` itself; Lua-driven mutations
flow through `with_app(|app| app.ui.theme())`.

`crate::theme::*` after P1.0:
- `populate_ui_theme(&mut Theme)` — initializes `Smelt*` groups from
  `is_light` / `accent` / `slug`.
- `detect_background(&mut Theme)` — OSC 11 / `$COLORFGBG` probe.
- `PRESETS` + `preset_by_name` — accent presets.
- `DEFAULT_ACCENT` re-export from `ui::theme::DEFAULT_ACCENT`.

42 call-site migrations, `Snapshot` snapshot type eliminated,
`ColorRole` expanded with Agent/Success/ErrorMsg/Apply/Plan/Exec/
Heading/ReasonLow/Med/High/Max. Syntect's light/dark hint moved to
`content/highlight/mod.rs` as a self-contained mirror updated by
`populate_ui_theme()` (avoids threading `&Theme` through 14 syntax
call sites for one branch).

### P1.a — Buffer foundation (laid; structural completion deferred)

Landed: `50b8923`, `67603de`, `26701d3`, `385e9d0`. Foundation
shipped: extmark + namespace model, YankSubst, soft-wrap cache,
`BufferParser` rename + `on_attach` hook.

Planned (per REFACTOR.md § P1.a): replace `lines`/`spans`/`marks`
with extmark model; add `Buffer::attach(spec)` parser hooks;
delete `transcript_cache.rs` + `BufferFormatter` trait; merge
`edit_buffer.rs` into `Buffer`.

Shipped:
- `ui::Buffer` storage is now `BTreeMap<ExtmarkId, Extmark>` per
  `Namespace`. `add_highlight` / `set_decoration` / `set_virtual_text`
  / `set_mark` are thin wrappers over `set_extmark` in well-known
  namespaces (`NS_HIGHLIGHTS`, `NS_DECORATIONS`, `NS_VIRT_TEXT`,
  `NS_MARKS`).
- `YankSubst::{ Empty | Static(String) }` extmark field +
  `Buffer::yank_text_for_range` helper. No callers yet (intended
  consumers — hidden-thinking elision, attachment expansion — turned
  out to be wrong fits; see Decisions).
- `Buffer::wrap_at(width)` soft-wrap cache keyed by `(changedtick,
  width)`. No callers yet (rendering is pre-wrapped via DisplayLine).
- `BufferFormatter` trait → `BufferParser`; `render` method →
  `parse`; `with_formatter` builder → `attach`; `set_formatter` →
  `set_parser`. `BufferParser::on_attach(&mut Buffer)` lifecycle
  hook (default no-op) for parsers to register custom namespaces.

Deferred to P1.a-tail (gated on transcript-pipeline migration onto
`BufferParser`, itself a multi-session keystone):
- `Buffer::attach(spec)` parser-hook system replacing `BufferFormatter`
  trait + `transcript_cache.rs` IR cache.
- Transcript renderers (`transcript_present/*.rs`) onto `BufferParser`
  — each `Block` kind becomes its own parser, `BlockArtifact` becomes
  per-block Buffer, `TranscriptSnapshot` composes from per-block
  Buffers.
- `transcript_cache.rs` deletion (per-parser IR caches replace it).
- `edit_buffer.rs` merge into `Buffer` (~250 references; pairs
  naturally with P1.d when vim state machine decomposes).

### P1.b — LayoutTree ✅

Landed: B.1 → B.4 (`70ad2e0`, `5e1d81c`, `14612d8`, `e615c12`,
`2a157da`).

Planned (per REFACTOR.md § P1.b): `Vbox | Hbox | Leaf(WinId)` with
chrome on containers; `Item = (Constraint, LayoutTree)`; constraints
`Length / Percentage / Ratio / Min / Max / Fill / Fit`.

Shipped exactly that:
- B.1 — `Constraint` expanded from `{Fixed, Pct, Fill}` to the seven
  target variants. Three-pass resolution (hard sizes → ratios →
  fill). `Fit` stubbed to `Fill` until leaves expose natural size.
  Renamed `Fixed → Length`, `Pct → Percentage`. 8 consumer files
  updated.
- B.2 — `gap`/`border`/`title` chrome on `Split`; `vbox`/`hbox`/`leaf`
  constructors and `with_gap`/`with_border`/`with_title` builders.
  All in-file tests rewritten on the constructors. Dummy "gap"
  leaf in `content/layout.rs` replaced with `.with_gap(1)`.
- B.3 — `Item = (Constraint, LayoutTree)`; constraint hoisted out
  of `Leaf` onto parent `items`. `Leaf(WinId)` (string names gone).
  `resolve_layout` returns `HashMap<WinId, Rect>`. Production
  consumers use `TRANSCRIPT_WIN`/`PROMPT_WIN` constants.
- B.4 — `Split` replaced with explicit `Vbox`/`Hbox` variants
  sharing a `Chrome` struct. `Direction` enum deleted.

Deviation from spec: `SeparatorStyle` (planned as a `Chrome`
field) deferred — needs new enum + render-time wiring; will land
in P1.c alongside Overlay chrome.

### P1.c — Overlay replacing Float (in progress; foundation landed)

Per REFACTOR.md § P1.c: `Overlay { layout: LayoutTree, anchor:
Anchor, z: u16, modal: bool }` with rich `Anchor` enum
(`ScreenCenter` / `ScreenAt` / `Cursor` / `Win { target, attach }`).
Deletes `Float`/`FloatId`, `Placement`, `PanelWidget` trait +
`dialog.rs` panel multiplexing.

Foundation shipped:
- C.0 (`40f0c82`) — Renamed legacy `Anchor` (NW/NE/SW/SE corner
  enum) → `Corner`. Frees the `Anchor` name for the target richer
  enum. 9 references updated; pure rename.
- C.1 (`702305a`) — Added target `Anchor` enum (`ScreenCenter` /
  `ScreenAt { row, col, corner }` / `Cursor { corner, row_offset,
  col_offset }` / `Win { target, attach }`). Added `Overlay`
  struct (`layout` / `anchor` / `z` / `modal` + builders) and
  `OverlayId` newtype in a new `overlay` module. 3 unit tests.
- C.2 (`8fa6760`) — `resolve_anchor(anchor, size, ctx) -> Option<Rect>`
  pure function with `AnchorContext { term_width, term_height,
  cursor, win_rects }`. Cursor anchors flip to the opposite corner
  on overflow (canonical popup behavior); Win anchors return `None`
  when target rect is missing; all results clamp to terminal. 9
  unit tests covering each variant + clamp + flip + missing-target.
- C.3 (`d3c4a83`) — `Ui::overlay_open / overlay_close / overlay /
  overlay_mut / overlays_in_z_order` plus `overlays:
  HashMap<OverlayId, Overlay>` storage. Stable `OverlayId`s minted
  per open. 4 unit tests: id uniqueness, close idempotence,
  anchor-mutation (drag), z-order sort with insertion-order tiebreak.
- C.4 (`44fe779`) — Per-frame resolution layer.
  `LayoutTree::natural_size((cap_w, cap_h)) -> (u16, u16)` walks
  the tree and adds up `Length` / `Percentage` / `Ratio` / `Min` /
  `Max` along the parent axis (max across siblings on the
  secondary axis), then layers on `Chrome` (border = +2 each axis;
  gap = (n-1) × gap on primary). `Fill` / `Fit` contribute `0` —
  content-natural sizing waits for P1.d when leaves expose Buffer
  extent. Result clamps to `cap`. `Ui::resolve_overlays(cursor)`
  walks `overlays_in_z_order`, resolves each via `resolve_anchor`
  with the natural size and the caller-supplied cursor +
  `split_rects`, and returns `Vec<(OverlayId, Rect, &Overlay)>`
  for resolvable entries. Cursor-anchored overlays drop out when
  `cursor` is `None`; `Win`-anchored overlays drop out when the
  target rect isn't published. 13 unit tests (9 covering
  `natural_size` axis composition + chrome + cap clamp; 4 covering
  `resolve_overlays` skip + z-order + center).
- C.4-tail (`434eee8`, `16ca777`) — `SeparatorStyle` (None /
  Solid / Dashed) lands on `Chrome` as the fourth chrome property
  called out in ARCHITECTURE.md. `LayoutTree::with_separator(s)`
  auto-inflates `gap = 0 → 1` when `s != None` so callers can opt
  in without manually budgeting space. The dialog-side
  `SeparatorStyle` enum (Solid/Dashed only, used as
  `Option<SeparatorStyle>`) was deduplicated against the new
  layout enum: `dialog::SeparatorStyle` deleted, `PanelSpec.
  separator_above` switched from `Option<…>` to bare
  `SeparatorStyle` (default `None`). Render call sites match on
  the three-variant enum. 5 new unit tests + paint behavior
  preserved (dialog still draws Dashed/Solid the same way; the
  new layout-side `separator` is data-only until Ui-facade
  rendering lands in P1.f).
- C.4-tail₂ (`7cee24c`) — focus + hit-testing primitives for
  overlays. `Ui::active_modal() -> Option<OverlayId>` returns the
  topmost overlay with `modal=true` (insertion-order tiebreak via
  `OverlayId`), independent of focus — feeds engine-drain gating
  + modal-aware Tab cycling later. `OverlayHitTarget::{ Window
  (WinId) | Chrome }` distinguishes leaf hits from chrome hits
  (border / title / gap / padding). `Ui::overlay_hit_test(row,
  col, cursor) -> Option<(OverlayId, OverlayHitTarget)>` walks
  `resolve_overlays` topmost-first, finds the first overlay
  containing the point, then uses `resolve_layout` to decide
  Window vs Chrome. Modal-aware: when a modal is open, only the
  modal and overlays at or above its `z` accept hits (lower-z
  overlays return `None` even if their rect contains the point).
  9 unit tests (3 `active_modal` + 6 `overlay_hit_test` covering
  empty / leaf / chrome / outside / modal-block / topmost-wins).
- C.4-tail₃ (`d94d12c`) — canonical Win-typed focus API per
  ARCHITECTURE.md. `Ui::focus() -> Option<WinId>` reads through
  the compositor's focused layer and translates back via
  `layer_to_win`. `Ui::set_focus(WinId) -> bool` looks up the
  win's compositor layer (split via `register_split` map; float
  via the `"float:N"` prefix when actually registered), pushes
  the prior focus onto `focus_history`, and switches focus.
  Returns `false` when the win has no layer. Re-focusing the
  current win is a no-op (no history push). `focus_history`
  field added to `Ui`; `focus_history()` accessor returns a
  borrowed slice. `set_focus` is the single new entry point;
  the old `focus_layer(id)` stays as the layer-id escape hatch
  until P1.f's facade rewrite. 6 unit tests (fresh / unknown /
  registered split / prior push / no-op same-win / chain).
- C.4-tail₄ (`2713f01`, `50e2ba5`) — overlay/focus structural
  glue. `LayoutTree::contains_leaf(WinId) -> bool` walks the
  tree depth-first; pure structural query, no rect math.
  `Ui::focused_overlay() -> Option<OverlayId>` returns the
  overlay whose layout contains `focus()` as a leaf — None
  when focus is on a split or nothing is focused.
  `Ui::focused_window()` / `focused_window_mut()` are
  conveniences over `focus()` that resolve the `Window` struct
  for callers that need cursor / config / state. `Ui::overlay_
  close` now implements ARCHITECTURE.md's "pops focus_history"
  contract: when focus is currently on a leaf of the closed
  overlay, walks `focus_history` backward to the most recent
  still-focusable `WinId` and restores focus there; on
  exhausted history, calls `Compositor::clear_focus()` (new
  API) so stale layer ids don't lurk. 9 unit tests
  (`contains_leaf` × 2, `focused_overlay` × 3, `focused_window`
  × 1, `overlay_close` close-pop × 3).
- C.4-tail₅ (`dcb0e8b`) — unified `Ui::hit_test`. New
  `HitTarget::{ Window(WinId) | Scrollbar { owner: WinId } |
  Chrome { owner: OverlayId } }` enum (the spec shape from
  ARCHITECTURE.md). `Ui::hit_test(row, col, cursor) ->
  Option<HitTarget>` checks overlays first via
  `overlay_hit_test` (modal-aware, topmost-z) and falls back to
  the compositor's split/float lookup. `Scrollbar` is reserved
  for P1.d when Window publishes its scrollbar rect — the
  variant exists in the enum so consumers pattern-match the
  full target shape today, but `hit_test` doesn't return it
  yet. Renamed `overlay_focused → focused_overlay` to match
  ARCHITECTURE.md naming. 3 unit tests (overlay leaf / overlay
  chrome / no-cover-returns-None).
- C.4-tail₆ (`f80d1d0`) — modal-aware Tab cycling.
  `LayoutTree::leaves_in_order() -> Vec<WinId>` walks the tree
  depth-first and returns leaf `WinId`s in declaration order
  (the order the user sees them when reading the layout
  source). `Ui::focus_next() -> bool` / `focus_prev() -> bool`
  cycle the active modal's focusable leaves (filtered by
  registered compositor layer); wrap with `rem_euclid`. Both
  return `false` when no modal is active — non-modal
  cross-source z-order cycling is gated on the unified Ui
  facade in P1.f. Closes the last spec API entry from
  ARCHITECTURE.md § Focus / hit-testing for the data layer.
  6 unit tests (`leaves_in_order_walks_depth_first`,
  `leaves_in_order_single_leaf`, `focus_next_returns_false_outside_modal`,
  `focus_next_cycles_modal_leaves`, `focus_prev_walks_backwards_with_wrap`,
  `focus_next_skips_unregistered_leaves`).
- C.5.0 (`5a467d5`) — `Compositor::render_with` overlay-paint
  hook. New variant of `render` takes an `after_layers:
  FnOnce(&mut Grid, &Theme)` closure that runs between layer
  paint and cursor placement / flush. The closure receives the
  in-flight `current` grid plus the borrowed theme so it can
  resolve highlight ids. `render` itself becomes a thin
  no-op-closure call, preserving the existing single-call
  surface for callers that don't use overlays. Unblocks the
  paint pass for overlays (which are managed by `Ui`, not the
  compositor's layer registry). 1 unit test
  (`render_with_paints_after_layers`).
- C.5.1 (`0836ae1`) — minimal `Window::render(&self, buf:
  &Buffer, slice: &mut GridSlice, ctx: &DrawContext)`. Paints
  visible buffer lines from `scroll_top` into the slice;
  read-only viewer scope (no extmark layered paint, no
  transient selection, no scrollbar, no gutters or per-line
  decoration). Truncates lines at slice width; stops when the
  buffer runs short. The first pin for P1.d's Window
  decomposition — the richer surface folds in alongside the
  `BufferView` deletion. 3 unit tests
  (`render_paints_visible_lines_from_scroll_top`,
  `render_truncates_at_slice_width`,
  `render_stops_when_buffer_runs_short`).
- C.5.2 (`41432a8`) — `paint_chrome(grid, area, chrome,
  theme)` free function in `layout` (border + title; padded
  corners + sides per `BorderStyle`; title sits in the top
  border row truncated to inner width). `Ui::render` rewires
  to `compositor.render_with(theme, w, |grid, theme|
  paint_overlay(...))`, walking the resolved-overlay set
  computed pre-render (cursor=None for now; cursor-anchored
  overlays are deferred until App publishes the prompt cursor
  position). The paint pass recurses the `LayoutTree`:
  containers paint chrome and recurse into children at their
  resolved rects; leaves slice the grid and call
  `Window::render`. `layout::resolve_constraints` lifted to
  `pub(crate)` so the paint walker shares the resolution
  logic with `resolve_layout`. 5 unit tests (4 `paint_chrome_*`
  + 1 `render_paints_overlay_leaf_buffer` end-to-end).
- C.5.3 (`d77d513`) — first float migrated to Overlay.
  `text_modal` (used by `/stats` and `/cost`) becomes
  `Overlay { layout: vbox(Percentage(60), hbox(Percentage(70),
  leaf(WinId))).with_border(Single).with_title(...), anchor:
  ScreenCenter, modal: true }`. Esc dismiss wired via a
  built-in `Ui::handle_key` precedence: when `active_modal()`
  is some, bare Esc closes the topmost modal and returns
  `Consumed`. `events.rs` gate for the float-key dispatch
  branch widened from `focused_float().is_some()` to also
  fire when `active_modal().is_some()` so events reach
  `handle_key_with_lua` while a modal overlay is open.
  Universal dismiss is fundamental behaviour and not
  user-customisable. 2 unit tests
  (`handle_key_esc_closes_active_modal`,
  `handle_key_esc_with_modifiers_does_not_dismiss_modal`)
  + manual TUI parity walk.
- C.5.4 (`0922dd0`) — overlay-leaf focus + leaf-key dispatch.
  New `Ui::overlay_focus: Option<WinId>` field tracks
  focus on overlay leaves (which are not compositor layers,
  so `compositor.focused()` can't represent them).
  `overlay_open(modal)` auto-focuses the layout's first
  leaf via `set_focus`. `set_focus` accepts overlay-leaf
  wins (looked up via `overlay_for_leaf(win)`, the public
  helper that walks `overlays` for leaves matching `win`).
  `Ui::focus()` prefers `overlay_focus` over compositor
  focus. `overlay_close` clears `overlay_focus` if affected
  and walks `focus_history` backward — preferring overlay
  leaves on prior overlays before splits/floats —
  restoring focus to the most recent still-existing
  target. `handle_key_with_lua` resolves the focused win
  via `focus()` instead of `compositor.focused()`, so
  callbacks registered against an overlay leaf's WinId
  fire normally. text_modal restores `q` + `Ctrl+C`
  dismiss via `win_set_keymap` Rust callbacks that resolve
  the containing overlay through `overlay_for_leaf` and
  call `overlay_close`. 3 unit tests
  (`overlay_open_modal_focuses_first_leaf`,
  `set_focus_accepts_overlay_leaf`,
  `handle_key_routes_to_overlay_leaf_callback`) + manual
  TUI parity walk (Esc / q / Ctrl+C all dismiss `/stats`
  + `/cost`).

- C.6 (`0f829ce`, `7330088`) — content-only Lua dialogs
  (`/help`, `/btw`, and any future `smelt.ui.dialog.open`
  call whose panel set is exclusively `kind="content"` /
  `kind="markdown"` buffer panels) route through
  `open_dialog_via_overlay` → `Overlay { vbox(border+title,
  vbox(leaves)), ScreenCenter, modal: true }`. The Lua
  surface (`dialog.lua`) is unchanged: callers still get a
  `win_id` and register `on_event("dismiss"|"submit"|
  "text_changed"|"tick")` and keymap callbacks against it.
  Two internal-API additions paired with the migration:
  (a) `Ui::win_close` on an overlay-leaf WinId now closes
  the containing overlay and clears callbacks for every
  leaf atomically (so a single `smelt.win.close(win_id)`
  call drops the whole dialog's Lua-side callback registry
  without per-leaf bookkeeping); (b) modal-Esc fires
  `WinEvent::Dismiss` on every leaf before closing the
  overlay so `dialog.lua`'s `on_event("dismiss", …)`
  handlers flush state (resume the parked task with
  `action="dismiss"`). Renderer pre-pass added so each
  overlay leaf's Buffer gets `ensure_rendered_at(leaf_width)`
  before the immutable paint walk reads its lines —
  without it, parsers (Plain wrap, markdown, diff, syntax)
  populate lazily and the dialog renders empty until the
  next mutation. Mixed dialogs with `kind="options"` /
  `kind="input"` / `kind="list"` keep the legacy
  `dialog_open` path. 3 unit tests
  (`modal_esc_fires_dismiss_on_every_leaf_before_closing`,
  `win_close_on_overlay_leaf_closes_overlay_and_clears_all_leaves`,
  `render_drives_ensure_rendered_at_for_each_overlay_leaf`)
  + manual TUI parity walk (`/help` lists keybindings,
  `/btw` streams answer; both dismiss on Esc; `/permissions`
  / `/agents` / `/stats` / `/cost` unchanged).

- C.7.0 (`affcc48`) — Window cursor-row highlight foundation.
  New `Window::cursor_line_highlight: bool` field (default
  `false`). When opt-in is set and the Window is focused (via
  `DrawContext::focused`), `Window::render` paints the cursor
  row with the `CursorLine` theme group's background style;
  unfocused or opt-out renders unchanged. New theme group
  `CursorLine` (light: AnsiValue(253), dark: AnsiValue(237))
  populated by `populate_ui_theme`. The list-shape selected-
  row highlight reuses standard line painting — no separate
  selection-paint pass; the Window stays a pure viewer. 3 unit
  tests (`render_highlights_cursor_row_when_opted_in_and_focused`,
  `render_skips_cursor_highlight_without_opt_in`,
  `render_skips_cursor_highlight_when_unfocused`).
- C.7.0.5 (`280ac6f`) — `CallbackResult::Event(WinEvent,
  Payload)` lets a Rust keymap callback fire a WinEvent on
  the same Window after the keypress. `Ui::handle_key_with_lua`
  captures the follow-up from both keymap and key_fallback
  branches, then calls `dispatch_event(win, ev, payload,
  lua_invoke)` after the original dispatch returns — the
  borrow on the keymap entry is released before the
  WinEvent fans out, and the callback registry path stays
  the same. Unblocks Buffer-backed list leaves whose Enter
  binding fires `Submit { Selection { row } }` without
  punching `lua_invoke` through to every Rust callback. 1
  unit test
  (`callback_result_event_dispatches_winevent_after_keymap`).
  Side effect: `CallbackResult` lost its `Eq, PartialEq`
  derives (Payload doesn't impl Eq); no consumers compared
  it with `==`.
- C.7.1 (`73da0f1`) — `kind = "list"` panel kind in
  `ui_ops.rs`. New `LeafShape::{ Content | List }`
  discriminator parallel to `panel_specs` tracks per-leaf
  post-creation wiring. `kind = "list"` panels build a
  focusable `PanelSpec::content(buf, height)` and mark
  the leaf as `LeafShape::List`. After the overlay opens,
  `configure_list_leaf` sets `cursor_line_highlight = true`
  and registers Rust keymap callbacks for j/k/Up/Down/
  PageDown/PageUp/Home/End/g (each calls `move_cursor`
  with a delta and clamps to the buffer's line count) plus
  Enter (returns `CallbackResult::Event(WinEvent::Submit,
  Payload::Selection { index = scroll_top + cursor_line })`
  so `dialog.lua`'s `on_event(win, "submit", …)` resumes
  the parked task with the absolute selected row).
  `move_cursor` does the borrow dance — fetches `buf` id
  immutably, then reads `line_count` immutably, then
  `win_mut` for cursor mutation — to avoid an `&mut Ui`
  conflict. Default initial focus to the first list leaf
  when no panel sets `focus = true`. The per-Window list
  recipe lives in Rust (target shape says recipes are Lua,
  but list-list-Enter is fundamental enough to bind in
  Rust until P3.b's keymap-recipe registry takes shape).
  Unblocks the `/agents` (one-list-only) and `/resume`
  (input + list — still routes through legacy `dialog_open`)
  pre-existing-broken state. Manual TUI walk: `/resume`
  opens (was broken with "unknown panel kind: list");
  `/help`, `/btw`, `/stats`, `/cost` continue to work via
  the C.5 / C.6 paths.

Pending:
- C.7.2+ — full OptionList migration: multi-select
  (checkbox prefix), shortcut keys (1-9), formatted
  display (meta column + dim styling), padded rows.
  These are the residue of `OptionList` not yet covered
  by the minimal `kind = "list"` Buffer-backed leaf.
- C.8 — `TextInput` rewritten as a Buffer-backed Window
  (single editable line buffer + cursor + keymap recipe).
  Unblocks ask_user_question + the input rows of confirm.
- C.9 — Every dialog flips; delete `FloatConfig` /
  `PanelWidget` trait / `dialog.rs` panel multiplexing /
  `Placement` enum.

## Decisions made while coding

- **Theme registry collapses onto a single owned struct, no global
  atomics.** Original P1.0 plan kept the `tui::theme` atomics as the
  source of truth and mirrored them into `ui::Theme` per-frame.
  Mid-phase, we collapsed: `ui::Theme` itself owns `is_light`,
  `accent`, `slug` as fields; per-frame `populate_ui_theme()` reads
  those and rewrites the `Smelt*` groups. Lua API closures access
  theme via `with_app(|app| app.ui.theme())`. **Why:** removed a
  global mutable state path entirely; now `Ui` is the single owner.
- **Syntect light/dark hint stays as a self-contained atomic in
  `content/highlight/mod.rs`.** Threading `&Theme` through 14 syntect
  call sites for a single boolean was disproportionate. Kept a
  module-local atomic that `populate_ui_theme()` updates each frame.
- **P1.a closes as "foundation laid"; structural completion
  deferred.** The deeper P1.a deliverables (parser-hook `attach(spec)`,
  `transcript_cache.rs` deletion, `edit_buffer.rs` merge) are gated
  on the transcript-pipeline migration onto `BufferParser`, which
  is itself a multi-session keystone. Rather than block all of P1
  on it, closed P1.a as foundation and moved to P1.b (independent).
  See STATUS § "Deferred to P1.a-tail."
- **`YankSubst` consumers turned out to be wrong fits.** The two
  intended consumers (hidden-thinking elision, prompt attachment
  expansion) don't actually fit: hidden-thinking copy goes through
  `TranscriptSnapshot` which bypasses Buffer entirely; attachment
  expansion happens at submit, not on copy. Closed both as "wrong
  fit" in STATUS. The API still landed because the eventual
  consumers (transcript blocks once migrated to BufferParser) will
  use it.
- **`Buffer::wrap_at` has no caller yet.** Same situation —
  rendering pre-wraps via `DisplayLine` before reaching the buffer.
  Wrap state today still lives in `WindowView`. The cache is
  correct API shape; consumers come with the transcript migration.
- **B.2 used builder constructors instead of struct-literal
  field-defaults.** `Direction` has no sensible Default and existing
  code constructed `Split { direction, children }` literally.
  Adding chrome fields with `..Default::default()` would have
  required deriving `Default` on `Split` (awkward). Instead added
  `LayoutTree::vbox(items)` / `hbox(items)` / `leaf(win)` builders
  + `with_gap` / `with_border` / `with_title`. **Side effect:** the
  builders are exactly the eventual `Vbox`/`Hbox` variant
  constructors needed in B.4 — saved a second migration.
- **Replaced "gap" leaf with `.with_gap(1)` chrome.** `content/layout.rs`
  modeled inter-pane spacing as a third leaf named "gap" of `Length(1)`
  that nothing read. With chrome support landed in B.2, switched to
  `.with_gap(1)`. Cleans up a workaround and removes a dead leaf
  before B.3's WinId migration (one less non-window leaf to handle).
- **Single `SeparatorStyle` (in `layout`), no `Option<…>` wrapper.**
  When C.4-tail added `SeparatorStyle` to `Chrome`, the dialog
  module already had a same-named 2-variant enum used as
  `Option<SeparatorStyle>`. Two definitions of the same paint
  concept would split the surface and is a parallel implementation
  the meta-rules forbid. **Picked:** consolidated on a 3-variant
  layout-side enum (`None / Solid / Dashed`, default `None`),
  deleted the dialog enum, switched `PanelSpec.separator_above`
  from `Option<SeparatorStyle>` to bare `SeparatorStyle` with
  `None` as the absence marker. **Alternative considered:** keep
  both temporarily, alias one to the other. **Why this:** the
  whole point of the unification is that the layout module owns
  composition primitives; passing the same data through a Lua
  parser (`parse_separator`) that also returns `Option<…>` was
  noisier than a single enum.
- **C.5 stops at design note, no code shipped.** First float
  migration would need either a `Window::render` seam pulled
  forward from P1.d or a temporary Component-carrier on Overlay.
  Both have a non-trivial commit shape. Rather than rush a path
  that the tree will fight, captured the three options in STATUS
  and recommended path 1 (extract a small Window-render helper
  scoped to read-only viewer Windows). C.5 lands when that path
  is confirmed.
- **Test markers (`A`/`B`/`C` constants) instead of synthetic
  `WinId::new`.** Layout tests in B.3+ need `WinId` values that don't
  collide with `PROMPT_WIN(0)` / `TRANSCRIPT_WIN(1)`. Used module-level
  `const A: WinId = WinId(100)`-style markers. Cleaner read in tests
  than `WinId(2)` + comment, and the offset signals "synthetic test
  id, not a real reserved one."

## Files / types / functions changed

P1.0:
- Added: `crates/ui/src/theme.rs` (`Theme` registry type, accent/slug/light
  fields, presets); `ColorRole::{ Agent, Success, ErrorMsg, Apply, Plan,
  Exec, Heading, ReasonLow, ReasonMed, ReasonHigh, ReasonMax }`.
- Deleted: `crate::theme::Snapshot` snapshot type; `tui::theme` atomic
  globals (`BG_LIGHT`, `ACCENT`, `SLUG`, accessors).
- Restructured: 42 renderer call sites read `ctx.theme.get(group)`
  instead of host constants; `Ui` owns `theme: Theme` and threads it
  through `DrawContext`.

P1.a:
- Added: `ui::buffer::{ NsId, ExtmarkId, Extmark, ExtmarkPayload,
  ExtmarkOpts, YankSubst, WrapResult }`; `Buffer::{ create_namespace,
  set_extmark, del_extmark, clear_namespace, extmarks, yank_text_for_range,
  wrap_at }`; `BufferParser` trait + `on_attach` hook; well-known
  namespace constants `NS_HIGHLIGHTS` / `NS_DECORATIONS` / `NS_VIRT_TEXT` /
  `NS_MARKS`.
- Renamed: `BufferFormatter` → `BufferParser`; `render` → `parse`;
  `with_formatter` → `attach`; `set_formatter` → `set_parser`;
  `has_formatter` → `has_parser`. Tui-side `ModeFormatter` →
  `ModeParser`; `into_formatter` → `into_parser`.
- Restructured: `Buffer` storage moved to per-namespace
  `BTreeMap<ExtmarkId, Extmark>`; per-line getters
  (`highlights_at` / `decoration_at` / `virtual_text_at` / `get_mark`)
  read directly from extmarks; `highlights_arc` / `decorations_arc`
  materialize on demand with a `(changedtick + marks_tick)` cache.
  `BufferView::sync_from_buffer` and `dialog::build_panels` now
  take `&mut Buffer` / `&mut dyn BufferResolver`.

P1.b:
- Added: `Constraint::{ Min, Max, Ratio, Fit }`; `LayoutTree::{ Vbox,
  Hbox }` variants; `Chrome { gap, border, title }` struct; `Item =
  (Constraint, LayoutTree)` type alias; `LayoutTree::{ vbox, hbox,
  leaf, with_gap, with_border, with_title }` constructors / builders.
- Renamed: `Constraint::Fixed` → `Length`; `Constraint::Pct` →
  `Percentage`.
- Deleted: `LayoutTree::Split { direction, children }` (replaced by
  `Vbox`/`Hbox`); `Direction` enum; the dummy `gap` leaf in
  `content/layout.rs`.
- Restructured: `Leaf { name: String, constraint: Constraint }` →
  `Leaf(WinId)`; constraint hoisted onto parent `items`;
  `resolve_layout` returns `HashMap<WinId, Rect>` instead of
  `HashMap<String, Rect>`. `Ui::resolve_splits` updated to match.

## Deferrals

To P1.a-tail (sub-phase to schedule once the transcript-pipeline
migration unblocks it):
- `Buffer::attach(spec)` parser-hook system + `BufferFormatter` trait
  deletion (the deeper half of P1.a).
- `transcript_cache.rs` deletion (per-parser IR caches replace it).
- `edit_buffer.rs` merge into `Buffer` (high-volume; ~250 references;
  pairs naturally with P1.d).
- `Buffer::yank_text_for_range` consumers (transcript blocks once
  migrated).
- `Buffer::wrap_at` consumers (same).
- `BufferView` `Arc::clone` removal (blocked on `BufferView` deletion
  in P1.d).

To P1.c:
- `SeparatorStyle` chrome field on `Vbox`/`Hbox` (originally planned in
  P1.b § chrome). Needs a new enum + render-time wiring; lands
  alongside Overlay chrome.

## Verification

At the close of each shipped sub-phase:
- P1.0 close: tree green; tests run on commit `e489a79`.
- P1.a foundation close: tree green at `9d5ac40`; 930 workspace tests
  passing (914 baseline + 16 covering extmark CRUD, yank substitution,
  wrap caching).
- P1.b close: tree green at `2a157da`; 939 workspace tests passing
  (930 baseline + 9 covering chrome / `Min` / `Max` / `Ratio` / `Fit` /
  gap-and-border composition). `cargo clippy --workspace --all-targets
  -- -D warnings` clean. 6 baseline scenarios still gating.
- `refactor/check.sh`: green at each boundary.

## Next phase entry conditions

P1.c can start as soon as P1.b lands (no shared state with the
deferred P1.a-tail). Entry conditions:
- `ui::Theme` available from `DrawContext` ✅ (P1.0).
- `LayoutTree` in target shape (Vbox/Hbox/Leaf(WinId) + chrome) ✅
  (P1.b).
- `Buffer` extmark model in place ✅ (P1.a foundation).

P1.d (Window) entry conditions: P1.c overlays exist so the
`Component`/`PanelWidget` deletion has the replacement structure to
point at.
