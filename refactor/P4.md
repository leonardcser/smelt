# P4 — Lua takes the UX

**Status:** in-progress **Started:** 2026-04-30 **Landed:** —

P4 moves every "what does smelt look/behave like" decision out of `tui`
into `runtime/lua/smelt/`. Rust shrinks to capability provision and
pixel pushing.

Sub-phases (interleave-friendly except for P4.b which is gated on
P1.a-tail `Buffer::attach`):

- **P4.a** — `runtime/lua/smelt/` layout. Create `widgets/`, `dialogs/`,
  `colorschemes/` directories and seed files. Move existing
  `plugins/*.lua` into their target homes (dialogs → `dialogs/`,
  picker recipes → `widgets/`).
- **P4.b** — transcript and diff parsers in Lua via `buf.attach { parser
  = "markdown", on_block = … }`. Gated on `Buffer::attach` (P1.a-tail).
  Drops `transcript_cache.rs`, `transcript_buf.rs`,
  `transcript_present/`, `content/highlight/`, `content/stream_parser`.
  This is also where `tui::parse` lands as the parse-only surface that
  Lua consumes through `smelt.parse` (or the per-language bindings —
  see P3 Decision `9d40423`).
- **P4.c** — reactive statusline. `status.lua` registers a segment spec
  via `smelt.statusline.set({ ... })`; segments bind to cells.
- **P4.d** — dialogs orchestrated in Lua. Each Rust dialog file
  collapses to a request-emit + a resolution primitive on the
  appropriate namespace; Lua composes the panels via
  `smelt.ui.dialog.open` (Overlay + LayoutTree + N Windows).
- **P4.e** — slash commands fully Lua. `runtime/lua/smelt/commands.lua`
  registers every builtin via `smelt.cmd.register`. `RUST_COMMANDS`
  table retires.
- **P4.f** — modes registry in Lua. `modes.lua` exposes `smelt.modes` —
  register a mode (name, display, keymap-overlay).

## Sub-phases landed

- **P4.a (`widgets/` seed: picker + prompt_picker)** (`d4bfedc`) — relocate top-level `runtime/lua/smelt/{picker,prompt_picker}.lua` into a fresh `runtime/lua/smelt/widgets/` directory; `BOOTSTRAP_CHUNKS` paths in `crates/tui/src/lua/mod.rs` updated to `smelt/widgets/{picker,prompt_picker}.lua`. First files in the new layout.
- **P4.a (`dialogs/` seed)** (`73056a5`) — relocate `runtime/lua/smelt/confirm.lua` (top-level bootstrap chunk) and `runtime/lua/smelt/plugins/{agents,permissions,resume,rewind}.lua` into a fresh `runtime/lua/smelt/dialogs/` directory; bootstrap path becomes `smelt/dialogs/confirm.lua`; `EMBEDDED_MODULES` keys + `AUTOLOAD_MODULES` entries rename from `smelt.plugins.*` to `smelt.dialogs.*` for the four migrated plugins. Behavior orchestration (Lua-driven Overlay + LayoutTree composition) stays P4.d.
- **P4.a (`colorschemes/` seed)** (`9bb2f11`) — create `runtime/lua/smelt/colorschemes/default.lua` (a `require`-able module that calls `smelt.theme.set("accent", { ansi = 208 })`) and register it as `smelt.colorschemes.default` via `EMBEDDED_MODULES`. Not autoloaded — the runtime default already paints ember-208 via `Theme::default + populate_ui_theme`; this seed is the pattern for plugin authors.
- **P4.a (`smelt.theme.use` sugar)** (`ebd901e`) — `_bootstrap.lua` adds `smelt.theme.use(name)` as `require("smelt.colorschemes." .. name)`. Lets `init.lua` write `smelt.theme.use("default")` per the ARCHITECTURE.md example.
- **P4.a (`smelt.theme.link` binding)** (`5d3cd46`) — `lua/api/theme.rs` exposes `smelt.theme.link(from, to)` as a thin wrapper over `ui::Theme::link`. Completes the `set` / `link` pair the colorscheme seed expects.
- **P4.d/P4.e (`/stats` + `/cost` to Lua)** (`11bd6c6`) — drop `cmd_stats` / `cmd_cost` from `RUST_COMMANDS`; delete `crates/tui/src/app/dialogs/text_modal.rs` (now unused); add `smelt.metrics.{stats_text,session_cost_text}` formatting helpers; new `runtime/lua/smelt/plugins/stats.lua` registers both commands and opens dismissable `smelt.ui.dialog.open` modals. First built-in commands to leave `RUST_COMMANDS`; first dialog file to retire entirely.
- **P4.e (`/clear`, `/new`, `/fork`, `/branch` to Lua)** (`04f6419`) — drop `cmd_clear` / `cmd_fork` from `RUST_COMMANDS`; add `smelt.session.{fork,reset}` Host-tier bindings; new `runtime/lua/smelt/plugins/session.lua` registers the four lifecycle commands. `/clear`'s Rust `CancelAndClear` path collapses into `session.reset`'s body so running-agent and idle paths share one implementation.
- **P4.e (`/quit` + 5 aliases to Lua)** (`7c028d8`) — drop `cmd_quit` and the six `exit`/`quit`/`q`/`qa`/`wq`/`wqa` rows from `RUST_COMMANDS`; expose top-level `smelt.quit()` that flips `TuiApp.pending_quit`; new `runtime/lua/smelt/plugins/quit.lua` registers the six aliases. Completer's `/`-picker filter now skips Lua commands declared with no description so the four hidden vim aliases stay out of the picker the way they did under `RustCommand { desc: None }`.
- **P4.e (`/compact` to Lua, RUST_COMMANDS empty)** (`abe9ed0`) — drop `cmd_compact` (the last entry) so `RUST_COMMANDS` is now `&[]`; new `runtime/lua/smelt/plugins/compact.lua` wraps the existing `smelt.engine.compact` Host-tier binding (which routes through `compact_or_notify`). RustCommand struct + handle_command lookup stay for one more session — Quit / CancelAndClear / Compact / Exec `CommandAction` variants are still consumed by the keyboard and shell-escape paths.
- **P4.e (retire `RUST_COMMANDS` infrastructure)** (`285ef4c`) — delete `RustCommand`, `RUST_COMMANDS`, `rust_command_items`, `is_rust_command`, and the Rust slash-command lookup in `handle_command`. `run_command` absorbs the leading `!` shell escape (the only surviving Rust path); everything else routes by name to a Lua handler or no-ops. `CommandAction` collapses to `{ Continue, Exec }`; `InputOutcome` drops `Quit` / `CancelAndClear` / `Compact`; `EventOutcome` drops `CancelAndClear`. The cmdline submit path reads `pending_quit` directly so closing the cmdline after `:q` still exits the app.
- **P4.e (gate while-busy via cmd opt)** (`68f8317`) — `RegisteredCommand` gains a `while_busy: bool` field (default `true`); `smelt.cmd.register` accepts an `opts.while_busy` flag. `try_command_while_running` consults `LuaRuntime::command_blocks_while_busy(name)` instead of the hardcoded `is_allowed_while_running` match. `is_allowed_while_running` and its tests retire; `plugins/compact.lua`, `plugins/session.lua` (`fork` / `branch`), and `dialogs/resume.lua` declare `while_busy = false`. `/branch` is now correctly blocked alongside `/fork` (was an oversight in the hardcoded list).
- **P4.e (`/reflect` + `/simplify` to Lua)** (`4420015`) — new `smelt.engine.submit_builtin_command(name, arg?)` Lua binding resolves a built-in prompt template (frontmatter overrides + minijinja `multi_agent` rendering) and starts a custom-command turn. New `plugins/{reflect,simplify}.lua` register the slash commands and delegate to the binding. `builtin_commands::list` and `is_builtin_command` retire; `custom_commands::resolve` / `is_custom_command` stop falling through to `builtin_commands` (built-ins are now Lua-registered like every other slash command); `completer::commands` drops its built-in enumeration. `begin_custom_command_turn` bumps to `pub(crate)` so the binding can start a turn directly.
- **P4.e (cmd `arg_hint` via Lua opts)** (`c9b98af`) — `RegisteredCommand` gains `arg_hint: Option<String>`; `cmd.register` accepts an `opts.arg_hint` string. `Completer::command_hint` consults `LuaRuntime::command_arg_hint(name)` first; the hardcoded `/btw` and `/compact` matches retire. `plugins/btw.lua` and `plugins/compact.lua` declare `arg_hint = "<question>"` / `"<instructions>"`.
- **P4.e (cmd `startup_ok` via Lua opts)** (`a4e278e`) — `RegisteredCommand` gains `startup_ok: bool` (default `false`); `cmd.register` accepts `opts.startup_ok`. The startup auto-submit dispatch consults `LuaRuntime::command_startup_ok(name)` instead of a hardcoded `"/resume" | "/settings"` match; `classify_startup_command` and its 5 tests retire. `dialogs/resume.lua` and `plugins/settings.lua` opt in; `smelt.cmd.picker` forwards `arg_hint` / `while_busy` / `startup_ok` to the underlying registration.
- **P4.e (`smelt.reasoning` namespace)** (`c2aa5b2`) — carve `smelt.reasoning.{get,set,cycle}` out of `smelt.engine` into a new top-level binding file `lua/api/reasoning.rs`, mirroring the `smelt.mode` shape (`ad9eccc`). Drops `smelt.engine.reasoning_effort` / `smelt.engine.set_reasoning_effort`; `cycle_reasoning` bumps from `pub(super)` to `pub(crate)`. No callers used the old surface (greenfield).
- **P4.e (`smelt.model` namespace)** (`844a0eb`) — carve `smelt.model.{get,set,list}` out of `smelt.engine` into `lua/api/model.rs`, aligning with the `smelt.model.set(name)` example in ARCHITECTURE.md. Drops `smelt.engine.{model,set_model,models}`; renames `models` → `list` for consistency with the get/set/list shape. `plugins/{model,export}.lua` re-point at the new namespace.
- **P4.e (session-scoped getters relocate)** (`e181467`) — move `smelt.engine.{cost,context_tokens,context_window}` to `smelt.session.{cost,context_tokens,context_window}`. The values are session state (`session.session_cost_usd` / `session.context_tokens`) plus a session-relevant config cap (`config.context_window`); the engine namespace ends as a turn-driver / event surface only. No external callers used the old surface.
- **P4.e (`smelt.session.messages`)** (`7424278` / `6aa8f2f`) — move `smelt.engine.history` → `smelt.session.messages`. The read is pure session state (`core.session.messages` is the SoT after P2.a.7b); the engine namespace ends as a turn-driver / event surface. `predict.lua` / `btw.lua` / `export.lua` re-pointed; `TurnEnd` doc-comment updated. `messages_to_lua` inlined into `session.rs` as a private fn (single caller after the carve).
- **P4.e (drop dead `smelt.engine.{submit,is_busy}`)** (`0ff8a7e`) — both bindings unused by any Lua plugin since the lua/api split (`submit` pushed onto `queued_messages` but the Rust-side keymap path writes that field directly; `is_busy` had no reader). Deleted per the greenfield rule. `smelt.engine` is now turn-driver writes (`cancel`, `compact`, `submit_builtin_command`) plus the `ask` primitive.
- **P4.e (generalize `:` -> `/` aliasing in dispatcher)** (`d8f1b9e`) — drop the hardcoded `matches!(input, ":q" | ":qa" | ":wq" | ":wqa")` filter in `try_command_while_running`. Any `:`-prefixed input now flows through the same dispatch path as `/`-prefixed input (`is_command` lookup, custom-command-queues-as-message, `while_busy` gate, `run_command`). Vim quit aliases are no longer special-cased; `:resume`, `:compact`, etc. now behave like their `/` siblings.

## Decisions made

- **`prompt_picker.lua` stays separate from `picker.lua`** (`d4bfedc`) — both relocate to `widgets/` as siblings. They share the "select-from-list" shape, but the prompt-docked variant is a non-focusable float that re-filters on every prompt keystroke (Up/Down nav lives in the prompt's vim recipe, not the picker), while the generic picker is a focusable modal with its own keymap. Merging would require a `mode = "modal"|"docked"` switch that gates half the body; the docked variant also routes through `smelt.prompt.open_picker` rather than `smelt.ui.picker.open`. Resolves the open INVENTORY question.

## Deferrals

(carried in from P3)

- **`tui::parse` + `smelt.parse`** lands with P4.b's transcript
  pipeline migration onto `BufferParser`.
- **`tui::subprocess` + `smelt.subprocess`** lands in P5.c with the
  engine multi-agent removal.
- **`tui::permissions` full absorption** lands in P5.c; the workspace
  JSON store half already shipped in P3 (`e34da06`,
  `tui::permissions::store`).
- **P2.a.10 (ToolRuntime carve-out)** lands with P5.a's
  `engine::ToolDispatcher` trait shape.
- **P2.b.5b / P2.b.5c** (TLS-slot generalization + `with_app` bulk
  migration) land with the first headless Lua driver in P5.b.
- **P2.d.2** full EngineBridge target fold lands with P1.a-tail
  `Buffer::attach`.
- **P1.d.5d** (vim registers / dot-repeat / undo → Buffer) lands
  with the `edit_buffer.rs` merge.
- **P1.d.5f.2d** + **P1.d.6c–e** (vim recipe-style flatten + completer
  recipe migration) land alongside the Lua keymap registry surface
  P3.b/P4 introduces.

## Open questions

## Next phase entry conditions

What must be true before `P5` starts.
