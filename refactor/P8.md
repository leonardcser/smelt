# P8 — Crate extraction

**Status:** in-progress **Started:** 2026-05-03 **Landed:**

## Completed

- **`ui` absorbed into `tui` (`tui/src/ui/`).** `crates/ui/` deleted;
  `crossterm` and `unicode-width` deps consolidated into
  `crates/tui/Cargo.toml`. ~550 `ui::` / `use ui::` references updated to
  `crate::ui::`.

- **`Clipboard` + `KillRing` moved to `core`.**
  `crates/tui/src/core/clipboard.rs` and `kill_ring.rs` created; `ui/mod.rs`
  re-exports from `core::clipboard`. `Core`, `Host`, and `commands.rs` updated
  to use `crate::core::Clipboard` / `crate::core::Sink`.

- **`core/core.rs` VimMode dependency removed.** `vim_mode` cell seeded with
  plain `"Insert"` string; `Core` no longer imports `ui::VimMode`.

- **`headless.rs` crossterm colors removed.** Tiny internal `AnsiColor` enum
  replaces `crossterm::style::Color` usage.

- **Terminal-specific submodule files moved from `core/` → `app/`.**
  `events.rs`, `mouse.rs`, `render_loop.rs`, `status_bar.rs`, `cmdline.rs`,
  `content_keys.rs`, `pane_focus.rs` moved to `tui/src/app/`. Visibility widened
  on `core/mod.rs` private items (`EventOutcome`, `Timers`, `InputOutcome`,
  `exec_rx`, `exec_kill`, `last_width`, `last_height`, `compact_epoch`) and on
  methods in `agent.rs`, `commands.rs`, `history.rs`, `lua_bridge.rs`,
  `working.rs` so the moved files can still access them. `core/mod.rs` crossterm
  imports reduced to only what the `run` method needs.

- **Remaining `impl TuiApp` / `ui`-dependent files moved from `core/` →
  `app/`.** `agent.rs`, `history.rs`, `lua_bridge.rs`, `lua_handlers.rs`,
  `transcript.rs` moved to `tui/src/app/`. Visibility widened on `core/mod.rs`
  items (`TurnState`, `TurnPhase`, `DeferredDialog`, `LoopAction`,
  `CONFIRM_DEFER_MS`, `pending_title`, `sleep_inhibit`, `next_turn_id`,
  `persister`, `pending_turn_meta`, `pending_compact_epoch`) and on `working.rs`
  methods (`begin`, `clear`, `elapsed`, `last_spinner_frame`,
  `set_last_spinner_frame`, `turn_meta`, `restore_from_turn_meta`).

- **Pre-existing `ui/` test compilation errors fixed.** When `ui` absorbed into
  `tui`, `#[cfg(test)]` blocks in `ui/flush.rs`, `ui/layout.rs`,
  `ui/overlay.rs`, `ui/window.rs`, `ui/vim.rs` retained `super::grid`,
  `super::buffer`, `super::theme`, `super::clipboard` references that only
  worked when `ui` was a standalone crate. All updated to `crate::ui::…` /
  `crate::core::…` paths.

- **Dead code and unused import sweep in `ui/mod.rs`.** Removed `NullSink`,
  `Sink`, `Span`, `SpanStyle`, `Cell`, `GridSlice`, `Anchor`, `SeparatorStyle`,
  `VimWindowState`, `BufferParser` from `ui/mod.rs` re-exports. Removed unused
  `UiHost::fire_win_event` and `UiHost::overlay_close` trait methods (and their
  impls) — callers use inherent methods on `Ui` directly. Removed unused
  `VirtualText::line` field and unused `vim::Action::EditInEditor` variant.

- **`term/` dissolved — headless-safe display model moved to `core/content/`,
  terminal adapters stay in `tui`.** `display.rs` decoupled from crossterm
  (`From<Color>` / `From<NamedColor>` impls removed; `to_buffer.rs` owns the
  `NamedColor → crossterm::Color` conversion). Headless-safe files moved:
  `context.rs`, `display.rs`, `stream_parser.rs`, `transcript.rs` →
  `core/content/`. `history.rs` → `core/history.rs`. Terminal-specific files
  relocated: `highlight/`, `layout.rs`, `layout_out.rs`, `prompt_data.rs`,
  `prompt_wrap.rs`, `selection.rs`, `status.rs`, `to_buffer.rs`,
  `transcript_buf.rs` → `tui/src/content/`; `input/` → `tui/src/input/`;
  `window.rs` → `tui/src/window.rs`. `term/` module deleted. All 80+
  `crate::term::` import sites updated across `core/`, `app/`, `lua/api/`,
  `format.rs`, `theme.rs`, `api.rs`. `INVENTORY.md` paths updated.
  `refactor/check.sh` fully green.

- **Clipboard sinks consolidated into `core/clipboard.rs`.** `SystemSink`,
  `Osc52Sink`, and the `copy_to_clipboard` / `paste_from_clipboard` helpers
  moved from `core/commands.rs` into `core/clipboard.rs`. Breaks the
  `commands.rs` ↔ `core.rs` module cycle.

- **Engine event dispatch moved to `app/engine_events.rs`.** `handle_event` and
  `handle_idle_event` relocated from `core/engine_client.rs` to
  `app/engine_events.rs` as `impl TuiApp` methods (`handle_engine_event`,
  `handle_idle_engine_event`). Breaks the `app.rs` ↔ `engine_client.rs` module
  cycle; `engine_client.rs` is now a pure `EngineHandle` wrapper.

## Revised task order

P8.a and P8.b were attempted out of order; the `TuiApp` extraction from
`core/mod.rs` was reverted because it broke `pub(super)` visibility across ~15
submodule files. The correct sequence is:

1. ✅ **Move terminal-specific submodule files out of `core/` first.**
   `events.rs`, `mouse.rs`, `render_loop.rs`, `status_bar.rs`, `cmdline.rs`,
   `content_keys.rs`, `pane_focus.rs` → `tui/src/app/`. Each is a self-contained
   `impl TuiApp` block with crossterm imports.

2. ✅ **Move remaining `impl TuiApp` / `ui`-dependent files out of `core/`.**
   `agent.rs`, `history.rs`, `lua_bridge.rs`, `lua_handlers.rs`, `transcript.rs`
   → `tui/src/app/`.

3. ✅ **Dissolve `term/content/` and `term/input/`.** Headless-safe display
   model → `core/content/`; terminal rendering adapters stay in `tui`.

4. ✅ **Extract `TuiApp` from `core/mod.rs`.** `TuiApp`, `WellKnown`,
   `AppFocus`, `TurnState`, `EventOutcome`, `CommandAction`, `InputOutcome`,
   `Timers`, `DeferredDialog`, `SessionControl`, `LoopAction`, `PendingTool`,
   and the main `run()` loop moved to `tui/src/app.rs`. `core/mod.rs` is now
   module declarations + re-exports only (~40 LOC). `app/mod.rs` deleted;
   `app.rs` + `app/` directory is the Rust 2018 module + submodules pattern.
   `lib.rs` changed `pub(crate) mod app` → `pub mod app` so the dead-code lint
   sees `TuiApp::run` as live (it is only called from the workspace-root
   binary).

5. ✅ **P8.c — Lua FFI tier split.** ~35 Host-tier bindings moved to
   `tui/src/core/lua/api/` (`au`, `cell`, `clipboard`, `cmd`, `engine`,
   `frontend`, `fs`, `fuzzy`, `grep`, `history`, `html`, `http`, `image`,
   `keymap`, `mcp`, `metrics`, `mode`, `model`, `os`, `parse`, `path`,
   `permissions`, `process`, `provider`, `reasoning`, `session`, `settings`,
   `shell`, `skills`, `spawn`, `task`, `timer`, `tools`, `transcript`, `vim`).
   `core/lua/api/mod.rs` provides
   `register_host_api(lua, smelt, smelt_keymap, shared)` which wires all
   Host-tier namespaces plus shared JSON helpers (`lua_table_to_json`,
   `json_to_lua_value`, `lua_table_to_args`) and the `app_read!` macro. ~11
   UiHost-tier bindings stay in `tui/src/lua/api/` (`bash`, `buf`, `confirm`,
   `diff`, `notebook`, `prompt`, `statusline`, `syntax`, `theme`, `ui`, `win`).
   `lua/api/mod.rs` calls `core::lua::api::register_host_api` first, then
   registers UiHost bindings and cross-cutting primitives (`notify`,
   `notify_error`, `quit`). `current_task_cancel` re-exported from `lua/mod.rs`
   so `core/lua/api/process.rs` can access it.

6. **P8.d — `with_app` returns `&mut dyn Host`.**

7. **P8.e — Physical crate split.** Create `crates/core/Cargo.toml`, move
   `tui/src/core/` → `crates/core/src/`, verify `cargo check -p core` succeeds
   with zero `crossterm` / `ui` imports.

## Decisions made

- **Lua FFI tier split (`core/lua/api/` vs `lua/api/`).** Host-tier bindings
  (subsystems, capabilities, config registries) live in `core/lua/api/`;
  UiHost-tier bindings (buffer/window/overlay operations, render primitives,
  theme access) stay in `lua/api/`. The split is organizational within the `tui`
  crate for now; `core/lua/api/` files still use `try_with_app` because the TLS
  slot still holds `TuiApp` (P8.d changes that to `&mut dyn Host`).
  `register_host_api` receives both `smelt` and `smelt_keymap` because
  `smelt.keymap` is a Host-tier namespace that writes to a dedicated sub-table.

- **`app/` renames to `core/` and capability modules consolidate (`37c153be`).**
  The `app/` module (P2 aggregate) is renamed to `core/` to match the target
  crate name. All generic capability modules (`fs`, `fuzzy`, `grep`, `html`,
  `http`, `notebook`, `path`, `permissions`, `process`) move inside `core/` so
  the eventual physical split only moves one directory. Terminal-specific files
  (`events.rs`, `mouse.rs`, `render_loop.rs`, `cmdline.rs`, etc.) stay in
  `core/` for now and move to `tui/src/` in P8.a.

- **Create `crates/core` now — not deferred.** The previous plan gated the
  physical split on a third frontend (GUI / server). That gate is removed; the
  split happens in P8 regardless. Two crates (`core` + `tui`) are strictly
  better than one internal module split, because they enforce the dependency
  arrow at the compiler level.

- **`crossterm` lives in `ui` and `tui`.** `protocol` and `engine` compile
  without it. `ui` is a terminal UI primitives crate — Buffer, Window, Grid,
  LayoutTree, VimMode — all inherently terminal-specific. A GUI or web frontend
  uses `core` directly and builds its own widgets; it has no use for `ui`.
  Keeping `crossterm` in `ui` makes `ui` a reusable TUI library rather than an
  awkward abstraction that satisfies no one. `core` has zero `crossterm` and
  zero `ui` imports.

- **`ui` absorbs into `tui`.** `ui` was factored out as a separate crate under
  the theory it could ship as a standalone TUI library. In practice it is too
  specialized (nvim-style extmarks, vim key dispatch, cell-grid rendering) and
  is tightly coupled to `tui` (529 `ui::` references, the `UiHost` trait
  implemented only by `TuiApp`). Four crates (`protocol`, `engine`, `core`,
  `tui`) is simpler than five. `ui` becomes `tui/src/ui/`.

- **`app.rs` is the module root, not `app/mod.rs`.** Rust 2018 supports both
  `foo.rs` + `foo/` and `foo/mod.rs`. The `app.rs` + `app/` pattern is chosen
  because `app.rs` carries the `TuiApp` struct and event loop, while `app/`
  carries the 12 `impl TuiApp` submodules. This keeps the module root file
  meaningful rather than a thin shim.

- **`pub mod app` over `pub(crate) mod app`.** The `tui` library's binary entry
  point lives in the workspace-root `src/main.rs`, not inside `crates/tui/`.
  When `app` is `pub(crate)`, rustc's dead-code lint considers `TuiApp::run`
  dead because no code inside the `tui` library calls it. Making `app` `pub`
  promotes `TuiApp::run` to the crate's public API surface from the lint's
  perspective, eliminating ~340 false-positive dead-code warnings.

- **`term/` dissolves — it is a transitional module.** `tui` is already the
  terminal crate; an extra `term/` layer inside it is redundant. The split that
  matters is `core` (headless) vs `tui` (terminal chrome), not `tui` vs
  `tui::term`. During P8:
  - `term/content/` splits: headless-safe display model (`DisplayBlock`,
    `LayoutContext`, `SpanCollector`, `stream_parser.rs`, `highlight/`) moves to
    `core/content/`; terminal rendering adapters (`to_buffer.rs`,
    `prompt_data.rs`, `status.rs`) stay in `tui`.
  - `term/input/` splits: data-model pieces (`History`, `PromptState`) move to
    `core`; terminal editing chrome (`completer_bridge.rs`, `vim_bridge.rs`,
    crossterm key dispatch in `mod.rs`) stays in `tui`.
  - `term/window.rs` stays in `tui`. This eliminates the circular dependency
    smell where `core` imports `crate::term::content::*` — a module that lives
    inside the `tui` crate.

## Deferrals

- None.

## Open questions — resolved

- **Clipboard.** `Clipboard`, `KillRing`, and the `Sink` trait move from `ui` to
  `core`. They are text-manipulation primitives, not UI rendering primitives —
  `KillRing` is pure `std`, `Sink` is a two-method platform abstraction, and
  `ui` itself (Buffer, Window, Grid) never touches them. `SystemSink` /
  `Osc52Sink` moved from `core/commands.rs` to `core/clipboard.rs` and implement
  `core::Sink`. This removes the `core → ui` dependency cleanly without
  inventing a wrapper trait.

- **VimMode.** `Core` stores `vim_mode` as a plain `String` cell (seeded
  `"Insert"`). `TuiApp` owns the typed `ui::VimMode` and publishes
  `format!("{:?}", vim_mode)` into the cell. Lua reads the cell string via
  `smelt.vim.mode`; it never sees the enum. This keeps the typed vim state
  machine in `ui` where it belongs while letting `core` stay ignorant of it.

## Next phase entry conditions

N/A — P8 is the terminal phase. When a third frontend appears it consumes
`crates/core` directly and implements its own chrome over `Core` + `Host`.
