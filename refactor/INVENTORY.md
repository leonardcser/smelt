# Inventory — current codebase state and target fate

Every source file in the repo, mapped against the refactor plan. Updated as
phases land. **This is the master list — when you're about to delete or move
something, check here first.**

Maintenance rules:

- When a phase lands, update the **Status** column for every row it touched
  (mark `done`, note the actual destination). **Status updates happen at
  phase boundaries, not per-commit.**
- When a fate decision changes mid-phase, update the row immediately and log the
  decision in the current `P<n>.md`.
- A new file added to the codebase = a new row here, in the same commit.
- "Unclear" rows demand a decision before the relevant phase begins.

Legend for **Fate**:

- `kept` — survives roughly as-is (may have internal edits).
- `renamed` — same logic, new name/path.
- `merged` — folds into another file/type.
- `restructured` — splits responsibilities across multiple new locations.
- `deleted` — gone entirely.
- `moved-to-lua` — responsibility moves to `runtime/lua/smelt/`.
- `moved-to-capability` — promoted to `tui::<name>` capability module.
- `unclear` — fate not yet decided; needs explicit decision before phase.

Legend for **Status**: `pending` (not yet touched), `in-progress`, `done`.

---

## `crates/tui/src/`

| File                  | LOC  | Purpose                                       | Fate         | Phase     | Status  | Notes                                                                                               |
| --------------------- | ---- | --------------------------------------------- | ------------ | --------- | ------- | --------------------------------------------------------------------------------------------------- |
| `ui/buffer.rs`           | 1201 | Lines, namespaced extmarks, virtual text | restructured | P1.a    | partial | `attach(spec)` parser-hook system + `BufferFormatter` deletion deferred to P1.a-tail (gated on transcript-pipeline migration). |
| `ui/callback.rs`         | 321  | Window callbacks + WinEvent routing           | kept         | P2        | pending | Drain pipeline for Lua callbacks; pattern preserved. |
| `ui/compositor.rs`       | 175  | Double-buffered renderer (grid + diff flush)  | restructured | P1.f      | partial | Renderer-only (`render_with` + grid diff). Final fold into `Ui` facade is P1.f. |
| `ui/event.rs`            | 107  | `Event` / `Status` / `FocusTarget` dispatch surface | added | P2.b.3 | landed | `Event` wraps crossterm payloads; `Status` replaces `DispatchOutcome` + `MouseAction`; `FocusTarget` is the semantic keyboard-focus alias. |
| `ui/flush.rs`            | 182  | Diff → SGR writer                             | kept         | P1        | pending | Grid diff persists as primitive                                                                     |
| `ui/grid.rs`             | 404  | Terminal frame (cells + style)                | kept         | P1        | pending | Primitive                                                                                           |
| `ui/id.rs`               | 39   | `BufId`, `WinId` newtypes                     | kept         | P1        | pending | Add `OverlayId`                                                                                     |
| `ui/layout.rs`           | 1098 | `LayoutTree` Vbox/Hbox/Leaf(WinId) + Constraint(7) + Chrome + Anchor(5) + natural_size + paint_chrome | restructured | P1.b/P1.c | partial | Target shape landed. |
| `ui/mod.rs`              | 3206 | `Ui` facade + `UiHost` trait                  | restructured | P1.f/P2.b.2/P2.b.3/P2.b.4c.3/P2.b.4c.5a/P2.b.4c.5b/P2.b.4c.6 | partial | Splits / overlays / focus / hit_test / capture / cursor_shape API operational; `dispatch_event(ui::Event) -> ui::Status` consumes the ui-owned terminal-event enum and now owns the scrollbar drag gesture; `Ui::resolve_split_mouse(MouseEvent) -> Option<(WinId, u8)>` latches `HitTarget::Window` capture on Down + records click count, returns the captured win on Drag/Up + clears capture on Up; `Ui::poll_drag_autoscroll() -> Option<(WinId, isize)>` reads the captured window's cursor_line + viewport height to drive per-tick autoscroll, with `drag_autoscroll_started()` exposing the engagement timestamp for the host's main-loop ramp; `hit_test` returns `HitTarget::Scrollbar` for splits leaves with painted bars; `UiHost` trait + `Ui` impl shipped in P2.b.2 + grew `viewport_for(WinId)` / `rows_for(WinId)` / `breaks_for(WinId)` per-pane accessors in P2.b.4c.5b (no `Host` supertrait). Full facade rewrite still ahead (F.6 tail). |
| `ui/overlay.rs`          | 455  | `Overlay` + `OverlayId` + `OverlayHitTarget` + `HitTarget` + `Anchor` resolver | added        | P1.c      | partial | Types + resolution + hit-test operational; legacy float surface gone (overlays + splits are the only containers). |
| `ui/text.rs`             | 372  | Text utilities (word boundaries, line ranges, transparent token ranges) | kept         | P1.a-tail | landed  | Absorbed `EditBuffer` free functions (`word_range_at_transparent`, `big_word_range_at_transparent`, `line_range_at`). |
| `ui/theme.rs`            | 204  | Highlight group registry (nvim-style)         | kept         | P1.0      | landed  | Replaces host `crate::theme::*` flat module; plumbed through DrawContext.                             |
| `ui/undo.rs`             | 66   | Undo/redo snapshots                           | merged       | P1.a      | pending | Into Buffer per-buffer edit history                                                                 |
| `ui/vim.rs`              | 3043 | Vim state machine                             | deleted      | P1.d.5    | partial | 5d (registers/dot-repeat/undo → Buffer) + 5f.2d (recipe-style flatten, gated on Lua keymap registry) still pending. |
| `ui/motions.rs`          | 333  | Cursor-motion primitives over `&str` (h/j/k/l/w/b/e/f/F/t/T/%, FindKind) | renamed      | P1.d.5f.2b | landed  | Top-level primitive. |
| `ui/text_objects.rs`     | 157  | Vim-shaped text-object selection (iw/aw/i"/a"/i(/a(, etc.) | renamed      | P1.d.5f.2b | landed  | Top-level primitive. |
| `ui/window.rs`           | 1804 | Window viewport                               | restructured | P1.d/P2.b.3/P2.b.4a/P2.b.4c.5c | partial | `render(buf, slice, ctx)` + cursor + scrollbar paint operational; `Window::handle(Event, EventCtx) -> Status` is the single public dispatch entry, with `handle_key` / `handle_mouse` now `pub(crate)` helpers; per-Window vim state inline (`vim_state`, `selection_anchor`, `curswant`); virt-text paints after highlights. Remaining decomposition waits for P1.d.5d (registers / dot-repeat / undo → Buffer) + P1.d.5f.2d (Lua keymap registry). |

## `crates/tui/src/`

| File                                      | LOC  | Purpose                                                     | Fate                | Phase     | Status  | Notes                                                                                                       |
| ----------------------------------------- | ---- | ----------------------------------------------------------- | ------------------- | --------- | ------- | ----------------------------------------------------------------------------------------------------------- |
| `alloc.rs`                                | 66   | Allocation tracking                                         | kept                | none      | pending | Utility                                                                                                     |
| `api.rs`                                  | 67   | Public crate API surface (`buf::replace` + `cmd::run`)      | kept                | P3        | pending | Dead `block::*` module retired in `49e4e49`; dead `buf::get_text` retired in `2e31dd0`; `buf::*` is the canonical "stuff text into the prompt" path; `cmd::*` is the slash-command entry point.                       |
| `core/mod.rs`                              | ~40  | Module declarations + re-exports only                       | restructured        | P8        | done    | `TuiApp` and all terminal-specific types extracted to `app.rs` in P8 step 4. `core` is now headless-safe module declarations + `pub use` re-exports. |
| `app/agent.rs`                            | 770  | Agent turn lifecycle                                        | kept                | P5.c/P8   | done    | Moved from `core/` to `app/` in P8. Single-agent turn driver; multi-agent branch deleted. |
| `core/app_config.rs`                       | 33   | `AppConfig` data carve-out (provider triple, mode + reasoning cycles, settings, model/cli overrides, context window) | kept                | P2.a.6    | landed  | Stays as a Core subsystem field after the P2.a.12 split. |
| `core/cells.rs`                            | 1122 | `Cells` reactive primitive (typed name → value + per-`TypeId` Lua projector + glob subs + queue-then-drain + `build_with_builtins`) | kept                | P2.a.4    | landed  | All built-in cells declared and projector-backed; `turn_complete` / `turn_error` publish call sites relocate alongside their handlers when the engine-event drain folds into `EngineClient` (P2.d). |
| `core/clipboard.rs`                        | 81   | Clipboard subsystem (kill ring + sink)                      | kept                | P8        | done    | Moved from `ui` to `core` in P8. `Clipboard`, `KillRing`, and `Sink` trait are text-manipulation primitives, not UI rendering primitives. `SystemSink` / `Osc52Sink` stay in `tui` and implement `core::Sink`. |
| `core/kill_ring.rs`                        | 241  | Kill ring                                                   | kept                | P8        | done    | Moved from `ui` to `core` in P8. Pure `std` data container; lives inside `core::Clipboard`. |
| `app.rs`                                   | ~1300| `TuiApp` struct + event loop + terminal-specific types      | restructured        | P8        | done    | Extracted from `core/mod.rs` in P8 step 4. Contains `TuiApp`, `WellKnown`, `AppFocus`, `TurnState`, `EventOutcome`, and the main `run()` loop; also declares `app/` submodules (`agent`, `cmdline`, `events`, `mouse`, etc.). |
| `app/cmdline.rs`                          | 431  | Cmdline buffer + key dispatcher (overlay path)              | moved-to-lua        | P4/P8    | done    | Moved from `core/` to `app/` in P8. Buffer-backed input leaf in a `ScreenBottom` modal overlay; P4 moves recipe to `widgets/cmdline.lua`. |
| `core/commands.rs`                         | 388  | `run_command` dispatcher + `apply_input_outcome` + shell escape | kept                | P4.e      | landed  | `RUST_COMMANDS` retired in `285ef4c`. `CommandAction` is now `{ Continue, Exec }`; the `!` shell-escape branch is the last surviving Rust path. |
| `core/confirms.rs`                         | 86   | `Confirms` subsystem (pending tool-approval requests)       | kept                | P2.a.3    | landed  | `is_clear()` is the engine-drain gate. Shares an `Arc<AtomicBool>` with `EngineClient` so the gate lives in one place (P6.b). |
| `core/core.rs`                             | 117  | `Core` aggregate of headless-safe subsystems (`config` / `session` / `confirms` / `clipboard` / `timers` / `cells` / `lua` / `engine` / `frontend: FrontendKind`); reached as `TuiApp.core.<field>` / `HeadlessApp.core.<field>` | kept                | P2.a.12a/P3.c  | landed  | `Core::new(config, engine, FrontendKind)` constructed by both `TuiApp::new` (Tui) and `HeadlessApp::new` (Headless). No separate `ToolRuntime` slot is required by the target; tool dispatch can stay on `LuaRuntime` unless a split-out runtime earns it. |
| `core/engine_client.rs`                    | 434  | Thin engine wrapper (`send / recv / try_recv / processes`) + free fns translating `EngineEvent` → `&mut TuiApp` mutations | restructured | P2.a.11/P2.d.1/P5.c | partial | Transitional middle state. `recv`/`try_recv` gate on `Confirms::is_clear()` via shared `Arc<AtomicBool>` (P6.b). Target is either a full event-to-buffer/cell bridge or no wrapper at all, with translation as plain reducer code. |
| `app/content_keys.rs`                     | 238  | Content pane keymap                                         | restructured        | P4/P8    | done    | Moved from `core/` to `app/` in P8. Becomes a Window keymap recipe in P4. |
| `app/events.rs`                           | 861  | Event dispatch (key/mouse/engine)                           | restructured        | P2.b/P2.e/P8 | done    | Moved from `core/` to `app/` in P8. Single `select!` loop fold still ahead (P2.e). |
| `core/headless.rs`                         | 228  | `HeadlessSink` (output writer for headless: format + verbose state, JSON / text / log helpers) | kept                | P2        | partial | Stays a thin sink wrapper around `HeadlessApp`'s output surface. |
| `core/headless_app.rs`                     | 244  | `HeadlessApp { core, sink, next_turn_id }` frontend: one-shot `run_oneshot` | kept                | P2.a.12b1 | landed  | First non-TUI frontend over `Core`. |
| `app/history.rs`                          | 623  | Rewind/fork/compact state                                   | merged              | P2/P8     | done    | Moved from `core/` to `app/` in P8. Reads/writes route through `self.session.*`. Final merge into Session subsystem stays P2.a.8+. |
| `core/host.rs`                             | 187  | `Host` + `UiHost` trait impls. `Host` is the Ui-agnostic accessor surface (`clipboard / cells / timers / lua / engine / session`) impl'd for `Core / TuiApp / HeadlessApp`. `UiHost` (the trait itself lives in `tui/src/ui/lib.rs`) is the compositor-bearing surface impl'd for `TuiApp` only. | restructured        | P2.b.1/P2.b.2/P2.b.4c.5b | partial | Keep both traits narrow. `confirms()` removed from `Host` trait — all consumers reach `core.confirms` directly (P6.b). The per-pane `viewport_for(WinId)` / `rows_for(WinId)` / `breaks_for(WinId)` overrides for PROMPT_WIN + TRANSCRIPT_WIN are transitional escape hatches while those surfaces still have tui-owned projections. |
| `app/lua_bridge.rs`                       | 152  | Lua FFI dispatch glue                                       | restructured        | P3/P8     | done    | Moved from `core/` to `app/` in P8. Per-namespace binding files                                                                                 |
| `app/lua_handlers.rs`                     | 131  | Lua async handlers                                          | kept                | P2/P3/P8  | done    | Moved from `core/` to `app/` in P8. Reads through `self.clipboard` + `self.session.messages`. |
| `app/mouse.rs`                            | 385  | Mouse routing + drag + scrollbar                            | restructured        | P1/P2/P8 | done    | Moved from `core/` to `app/` in P8. Viewport / rows / breaks reach through `ui::UiHost::{viewport_for, rows_for, breaks_for}` (`handle_content_mouse` fully migrated; `handle_prompt_mouse` reads viewport via the trait, keeps PromptWrap inline for src/wrapped translation); click-count via `Ui::record_click`; scrollbar gesture absorbed Ui-side (`propagate_scrollbar_scroll` mirrors the new `scroll_top`); wheel via `Ui::hit_test`; Down/Drag/Up Left for splits-leaf Windows fold onto `Ui::resolve_split_mouse` + `HitTarget::Window` capture (`dispatch_focused_mouse` / `extend_selection_to` retire); drag autoscroll edge detection lives on `Ui::poll_drag_autoscroll` (`tick_drag_autoscroll` collapses to per-pane forwarding for the transcript). Unified `Window::handle` fold waits on P2.b.4c.5c. |
| `app/pane_focus.rs`                       | 114  | Prompt vs Content focus cycling                             | restructured        | P2/P8    | done    | Moved from `core/` to `app/` in P8. Replaced by Ui::focus_next/focus_prev                                                                       |
| `app/render_loop.rs`                      | 447  | Per-frame render                                            | kept                | P2.e/P8  | done    | Moved from `core/` to `app/` in P8. Drives painted splits + cursor_shape per frame. Single `select!` loop hookup is P2.e. |
| `app/status_bar.rs`                       | 35   | Statusline buffer-write shell over Lua-driven spans         | moved-to-lua        | P4.c/P8  | done    | Moved from `core/` to `app/` in P8. `refresh_status_bar` collapsed to a thin wrapper: pulls items from `LuaRuntime::tick_statusline`, runs `spans_to_buffer_line`, writes the styled line into `well_known.statusline`. Composition lives in `runtime/lua/smelt/status.lua`. |
| `core/timers.rs`                           | 176  | `Timers` subsystem (scheduled Lua callbacks: one-shot + recurring) | kept                | P2.a.5    | landed  | Stays as a Core subsystem field after the P2.a.12 split. |
| `app/transcript.rs`                       | 763  | Transcript line/block management                            | restructured        | P2/P4/P8  | done    | Moved from `core/` to `app/` in P8. `transcript.lua` migration is P4.b. |
| `core/transcript_cache.rs`                 | 213  | Block layout cache                                          | deleted             | P4.b      | done    | Extmarks replace IR cache                                                                                   |
| `core/transcript_model.rs`                 | 877  | Block / ViewState / ToolState                               | restructured        | P2/P4     | pending | Splits into Session.history + on_block extmark population                                                   |
| `core/transcript_present/mod.rs`           | 1153 | Transcript render orchestrator                              | moved-to-lua        | P4.b      | done    | `transcript.lua` controller                                                                                 |
| `core/transcript_present/markdown.rs`      | 351  | Markdown presentation                                       | moved-to-lua        | P4.b      | done    | `transcript.lua` on_block + `core::parse`                                                                    |
| `core/transcript_present/tools.rs`         | 348  | Tool result formatting                                      | moved-to-lua        | P4.b      | done    | Lua extmarks                                                                                                |
| `core/transcript_present/tool_previews.rs` | 146  | Tool preview panels                                         | moved-to-lua        | P4.b      | done    | Lua extmarks                                                                                                |
| `core/working.rs`                          | 366  | Spinner + phase tracking                                    | merged              | P2.c/P2.d | done    | `spinner_frame` cell + Timers + EngineClient fires                                                          |
| `attachment.rs`                           | 237  | Prompt attachments                                          | restructured        | P1/P4     | pending | Extmarks in `attachments` namespace + Lua input widget                                                      |
| `completer/command.rs`                    | 64   | Command completion                                          | restructured        | P4        | pending | Lua input widget helper                                                                                     |
| `completer/file.rs`                       | 151  | File path completion                                        | restructured        | P4        | pending | Lua input widget helper                                                                                     |
| `completer/history.rs`                    | 158  | History completion                                          | restructured        | P4        | pending | Lua input widget helper                                                                                     |
| `completer/mod.rs`                        | 192  | Completer state machine                                     | restructured        | P1.d/P4   | pending | Decomposes: ghost-text extmark + Overlay picker + prompt keymap recipe                                      |
| `core/fuzzy/score.rs`                          | 62   | Completer ranking helpers (`query_match_score` / `split_words` / `recency_bonus`) | moved-to-capability | P3.a     | landed  | Folded under `core::fuzzy::score`; consumed by `completer::{mod,history}`.                                  |
| `config.rs`                               | 623  | Config types + model resolution + auxiliary routing         | merged              | P2.a/P5.d | landed  | `Deserialize` retired in P5.d; all config population is imperative (Lua APIs → `LuaShared` registries → `Config::from_lua_shared`). `SettingsConfig` / `AuxiliaryRouting` types stay here pending the full settings merge into `AppConfig`. |
| `core/content/context.rs`                      | 24   | Content context struct                                      | merged              | P1        | done    | Into Window render path                                                                                     |
| `core/content/display.rs`                      | 210  | Display rendering utility                                   | restructured        | P1        | pending | Window render pipeline                                                                                      |
| `content/highlight/diff.rs`               | 497  | Diff highlight logic                                        | restructured        | P3/P4     | pending | LCS to `core::parse`; presentation to `diff.lua`                                                             |
| `content/highlight/inline.rs`             | 1134 | Markdown / inline code parsing                              | restructured        | P3/P4     | pending | Parse to `core::parse`; extmark population in Lua                                                            |
| `content/highlight/mod.rs`                | 48   | Highlight module exports                                    | deleted             | P4        | done    | Replaced by Buffer.attach + extmarks                                                                        |
| `content/highlight/syntax.rs`             | 303  | syntect adapter                                             | moved-to-capability | P3        | done    | Into `core::parse`                                                                                           |
| `content/highlight/util.rs`               | 196  | Highlight utilities                                         | restructured        | P3/P4     | pending | Some to `core::parse`, some to Lua                                                                           |
| `content/layout.rs`                       | 198  | Content layout (transcript + prompt + status LayoutTree)    | restructured        | P1.b/P1.f.3 | landed  | `build_layout_tree(input, status_win)` + `LayoutState::from_ui`; status is a `Length(1)` leaf in an inner vbox. |
| `content/layout_out.rs`                   | 281  | Layout output                                               | restructured        | P1.b      | pending | Into Ui render path                                                                                         |
| `content/mod.rs`                          | 136  | Module exports                                              | deleted             | P4        | done    | Most submodules go away                                                                                     |
| `content/prompt_data.rs`                  | 1041 | Prompt content (formatting, completion)                     | restructured        | P2/P4     | partial | `compute_prompt` populates the prompt buffer + drives `wins[PROMPT_WIN]` cursor / viewport / completer virt-text. Final widget-recipe migration is P4. |
| `content/prompt_wrap.rs`                  | 233  | Prompt wrap state                                           | deleted             | P1.a      | done    | Wrap state lives on Buffer                                                                                  |
| `content/selection.rs`                    | 518  | Selection tracking                                          | restructured        | P1/P2     | pending | Window selection + theme.get("Visual")                                                                      |
| `content/status.rs`                       | 297  | Statusline span layout (priority / truncate / align)        | kept                | P4.c      | landed  | The Rust-side responsive layout engine — Lua composes segment lists; this file lays them out into a `StatusLine`. `StatusPosition` + `StyleState` re-export retired (Lua composer carries the position string itself; styles flow as plain fg/bg/bold). |
| `core/content/stream_parser.rs`                | 927  | Streaming markdown/diff parser                              | restructured        | P3/P6.a   | partial | 18 parse-marker trace tests added (chunk boundary independence, no-drop invariant, structural boundary detection). Target `Buffer::attach` + `on_block` callback still gated on P1.a-tail. |
| `content/to_buffer.rs`                    | 248  | Content → terminal buffer                                   | restructured        | P1        | pending | Window render to Grid                                                                                       |
| `core/content/transcript.rs`                   | 627  | Transcript display                                          | moved-to-lua        | P4.b      | done    | `transcript.lua`                                                                                            |
| `content/transcript_buf.rs`               | 75   | Transcript Buffer projection cache                          | deleted             | P4.b      | done    | Borrows the transcript display buffer; exports `NS_SELECTION`. Full deletion rides with `transcript.lua` migration in P4.b. |
| `lib.rs (content/input/window modules)`                                  | 15   | `term/` module root                                         | deleted             | P8        | pending | Dissolves in P8: `term/content/` splits to `core/content/` + `tui`, `term/input/` splits to `core` + `tui`, `window.rs` moves to `tui/src/`. |
| `custom_commands.rs`                      | 39   | `CustomCommand` / `CommandOverrides` / `RuleOverride` data types consumed by `begin_custom_command_turn` | moved-to-lua | P4.e | landed | File scanning, frontmatter parsing, exec-block evaluation, slash-command registration, and dispatch all live in `runtime/lua/smelt/plugins/custom_commands.lua` now; the Rust side keeps just the typed override structs the Lua binding fills. |
| `format.rs`                               | 276  | Text formatting helpers                                     | kept                | none      | pending | Utility                                                                                                     |
| `core/fs.rs`                                   | ~680 | Filesystem capability (sync std::fs primitives + `FileStateCache` + `try_flock` / `FlockGuard`) | kept | P3.a/P5 | done | Absorbed `engine/tools/file_state.rs` (`FileStateCache`, `file_mtime_ms`, `staleness_error`) and `engine/tools/mod.rs`'s `try_flock` / `FlockGuard`. |
| `core/fuzzy/mod.rs`                            | 143  | Fuzzy matching capability (`core::fuzzy::fuzzy_score`)       | kept                | P3.a      | landed  | Folder module; absorbed `completer/score.rs` as `fuzzy::score`. Dead `fuzzy_match` / `fuzzy_match_lower` retired in `2e31dd0`. |
| `core/grep.rs`                                 | 192  | Ripgrep wrapper (sync `Command` invocation, mode/glob/type/context options, timeout) | kept                | P3.a      | landed  | Pure subprocess composition, no policy. Engine tool absorption (`engine/tools/grep.rs`) waits on its migration to Lua in P5.b.                                                                          |
| `core/html.rs`                                 | 594  | HTML capability (title / links / to_text / `Markdown { title, content, links }` via `scraper`; `parse_ddg_results` for the DuckDuckGo HTML endpoint) | kept                | P3.a/P5.b | landed  | Read-only parse surface. `parse_ddg_results` landed alongside the `web_search` migration. The full HTML→markdown converter relocated from `engine/tools/web_shared.rs` (`html_to_md` + table renderer + blank-line collapse) lands as `to_markdown(html, base_url)` returning a `Markdown` aggregate alongside the `web_fetch` migration.            |
| `core/http.rs`                                 | 145  | HTTP capability (sync `reqwest::blocking` GET / POST, timeout / max_redirects / headers, response body+status+headers; `random_user_agent` rotator) | kept                | P3.a/P5.b | landed  | Pure transport. POST + UA rotator + the `cache` submodule landed alongside `web_search` migration. `Response::text()` retired with the `web_fetch` migration — Lua now reads the byte-safe body directly.                                                                  |
| `core/http/cache.rs`                           | 63   | Disk-backed TTL cache submodule (`get` / `put` / `put_with_ttl` over `<cache_dir>/web/<hash>`) | added              | P3.a/P5.b | landed  | Relocated from `engine/tools/web_cache.rs`; consumed by `web_search.lua` via `smelt.http.cache.{get,put}`.                                                                                                                                  |
| `input/buffer.rs`                         | 403  | Prompt edit buffer                                          | restructured        | P1.a/P4   | partial | Final merge into Buffer stays P1.a-tail. |
| `input/completer_bridge.rs`               | 281  | Completer integration                                       | restructured        | P1.d/P4   | pending | Ghost-text extmark + picker Overlay                                                                         |
| `core/history.rs`                        | 88   | Input history                                               | merged              | P2        | done    | Into Session.history                                                                                        |
| `input/mod.rs`                            | 2050 | Input subsystem (god-struct)                                | restructured        | P1/P2/P4  | partial | Carve into prompt Window + completer + attachments stays P1/P2/P4. |
| `input/vim_bridge.rs`                     | 101  | Vim mode in input                                           | restructured        | P2        | partial | Window keymap recipe still pending P2. |
| `instructions.rs`                         | 61   | System instructions model                                   | kept                | P2        | pending | Config data                                                                                                 |
| `keymap.rs`                               | 746  | Global keymap registry                                      | restructured        | P3/P4     | pending | Recipes are Lua; registry holds (mode, key) → recipe id. Dead `NavAction` enum + `nav_lookup` + 12 hint-fragment consts/fns retired in `2e31dd0`; `hints` module shrinks to the `help_sections` data feeding `smelt.keymap.help`. |
| `lib.rs`                                  | 34   | Crate root                                                  | kept                | none      | pending | Re-exports update as modules move. Dead `expand_at_refs` retired in `2e31dd0`.                              |
| `lua/api/mod.rs`                          | 569  | Lua API module organization                                 | restructured        | P3.b/P5.d | landed  | One file per top-level `smelt.<name>` namespace under `lua/api/`. Carries shared color / theme-role / json helpers; `register_api` walks every per-namespace `register` in turn. `state` / `widgets` / `dispatch` umbrellas all retired. Config-time namespaces (`provider`, `mcp`, `permissions`, `settings`) registered in P5.d. |
| `lua/api/au.rs`                           | 54   | `smelt.au.{on,fire}` thin alias over `smelt.cell` registry  | added               | P3.b      | landed  | Sugar layer; Host-tier (subscribe / set route through `try_with_app`).                                                       |
| `lua/api/bash.rs`                         | 36   | `smelt.bash.render(buf, command)` bash highlighter          | added               | P3.b      | landed  | Per-line gutter + `BashHighlighter` painted via `to_buffer::render_into_buffer`.                                              |
| `lua/api/buf.rs`                          | 197  | `smelt.buf.{create,set_lines,get_line,set_source,add_highlight,add_dim,text}` | added | P3.b | landed  | UiHost-only buffer surface; carries `smelt.buf.text` snapshot of the prompt.                                                  |
| `lua/api/cell.rs`                         | 205  | `smelt.cell` reactive registry bindings + `CellHandle` userdata | added           | P3.b      | landed  | Flat API + `smelt.cell(name)` userdata both reach `Cells`.                                                                   |
| `lua/api/clipboard.rs`                    | 47   | `smelt.clipboard.{read,write}` + legacy `__call` write       | added               | P3.b      | landed  | Read primitive added in this split (was write-only in `register_api`); routes through `core.clipboard.{read,write}`.    |
| `lua/api/cmd.rs`                          | 90   | `smelt.cmd.{register,run,list}` — slash command registry     | added               | P3.b      | landed  | Writes to `LuaShared.commands`. `register` opts: `desc / args / while_busy / queue_when_busy / startup_ok` (queue_when_busy added P4.e for user-defined custom commands so the dispatcher silently defers them when the agent is mid-turn). |
| `lua/api/diff.rs`                         | 33   | `smelt.diff.render(buf, { old, new, path })` inline diff     | added               | P3.b      | landed  | Reuses `print_inline_diff` + `to_buffer::render_into_buffer`.                                                                 |
| `lua/api/frontend.rs`                     | 33   | `smelt.frontend.{kind,is_interactive}` reads `Core.frontend` | added               | P3.c      | landed  | Today reads always return `Tui` since only `TuiApp` installs the TLS app pointer; switches to real dispatch with P2.b.5b.    |
| `lua/api/fs.rs`                           | 279  | `smelt.fs.{read,write,exists,is_file,is_dir,read_dir,mkdir,mkdir_all,remove_file,remove_dir,remove_dir_all,rename,copy,mtime,size}` | added | P3.c | landed | Host-tier; thin wrapper over `core::fs` using the `(value, err)` Lua convention. |
| `lua/api/os.rs`                           | 73   | `smelt.os.{getenv,setenv,unsetenv,platform,arch,tempdir,home,cwd,set_cwd,pid}` | added | P3.c | landed | Host-tier; pure Rust `std::env` surface — no Rust capability module backs it. |
| `lua/api/engine.rs`                       | 195  | `smelt.engine.{cancel,compact,submit_command,ask}` | restructured | P3.b/P5.c | partial | Turn-driver writes; `ask` opens the auxiliary-task channel via `LuaShared.callbacks`; `submit_command(name, body, overrides?)` starts a Lua-rendered slash-command turn (used by `/reflect`, `/simplify`, and the user-defined custom-commands plugin). Mode get/set/cycle live under `smelt.mode`; reasoning effort lives under `smelt.reasoning`; model get/set/list live under `smelt.model`; per-session cost / context-token / context-window / messages snapshot live under `smelt.session`. |
| `lua/api/mode.rs`                         | 37   | `smelt.mode.{get,set,cycle}` over `protocol::AgentMode`      | added               | P3.c      | landed  | Top-level namespace per Decision #9 (avoids future `smelt.process` long-lived IPC collision).                                              |
| `lua/api/model.rs`                        | 47   | `smelt.model.{get,set,list}` over the configured provider/model | added            | P4.e      | landed  | Carved from `smelt.engine` to align with ARCHITECTURE.md's `init.lua` example; `set` routes through `apply_model`.              |
| `lua/api/reasoning.rs`                    | 44   | `smelt.reasoning.{get,set,cycle}` over `protocol::ReasoningEffort` | added         | P4.e      | landed  | Carved from `smelt.engine` to mirror `smelt.mode`'s shape; `cycle_reasoning` bumped to `pub(crate)` for the binding.            |
| `lua/api/fuzzy.rs`                        | 19   | `smelt.fuzzy.score` thin Lua surface over `core::fuzzy::fuzzy_score` | added         | P3.b      | landed  | Folder-module capability is consumed via the binding here.                                                                    |
| `lua/api/grep.rs`                         | 70   | `smelt.grep.run(pattern, path, opts)` over `core::grep`       | added               | P3.c      | landed  | Host-tier; mirrors options table → `grep::Options`; output table carries `stdout / stderr / exit_code / timed_out`.           |
| `lua/api/history.rs`                      | 60   | `smelt.history.{entries,search}` past-prompt surface         | added               | P3.b      | landed  | Reads `core.input_history`; ranks via `completer::history::history_score`.                                                     |
| `lua/api/html.rs`                         | 60   | `smelt.html.{title,links,to_text,to_markdown,parse_ddg_results}` over `core::html` | added               | P3.c/P5.b | landed  | Host-tier; read-only HTML projections. `parse_ddg_results` returns `{ {title,link,description}, ... }`. `to_markdown(html, base_url)` returns `{ title, content, links }` — the consolidated payload `web_fetch.lua` feeds the LLM extractor.            |
| `lua/api/http.rs`                         | 93   | `smelt.http.{get,post,random_user_agent,cache.{get,put}}` over `core::http`                 | added               | P3.c/P5.b | landed  | Host-tier; sync GET / POST with timeout / max_redirects / headers; output table carries `status / final_url / headers / body`. `cache` submodule wraps `core::http::cache` (15 min disk-backed TTL).       |
| `lua/api/image.rs`                        | 30   | `smelt.image.{is_image_file,read_as_data_url}` over `engine::image` | added         | P5.b      | landed  | Backs `runtime/lua/smelt/tools/read_file.lua` for `.png/.jpg/.gif/.webp/.bmp/.tiff/.svg` short-circuit into a base64 data URL.  |
| `lua/api/keymap.rs`                       | 75   | `smelt.keymap.{set,help}` — chord registration + help sections | added            | P3.b      | landed  | Canonicalizes mode + chord at registration; writes `LuaShared.keymaps`.                                                      |
| `lua/api/mcp.rs`                          | 99   | `smelt.mcp.{register,list}` — config-time MCP server registration | added            | P5.d      | landed  | `register` populates `LuaShared.mcp_configs` with `McpServerConfig::Local`; picked up by `Config::from_lua_shared` before engine start. `list` returns registered server names. |
| `lua/api/metrics.rs`                      | 45   | `smelt.metrics.{stats_text,session_cost_text}` formatted text  | added               | P4.d/e    | landed  | Preformatted strings consumed by `runtime/lua/smelt/plugins/stats.lua` for the `/stats` and `/cost` dialogs.                |
| `lua/api/notebook.rs`                     | 228  | `smelt.notebook.{render,parse,is_notebook_path}` — preview render + Host-tier parse via `core::notebook` | added               | P3.b/P3.c | landed  | Render is UiHost-only; `parse` returns `{ nbformat, nbformat_minor, cells }` (cell carries `kind / id / source / execution_count`).                                                                                       |
| `lua/api/parse.rs`                        | 120  | `smelt.parse.frontmatter(content) -> table | nil, body` | added | P3.c/P4.e/P5.d | landed | Host-tier; splits a leading `---\n…\n---\n` YAML block via a manual subset parser (no `serde_yml`), returns it as a Lua table plus the remaining body. Markdown / diff / syntax block parsers ride P4.b's transcript pipeline. |
| `lua/api/path.rs`                         | 95   | `smelt.path.{normalize,canonical,relative,expand,join,parent,basename,extension,is_absolute,config_dir,commands_dir}` | added | P3.c | landed  | Host-tier; thin wrapper over `core::path` plus a few `std::path::Path` accessors and `config_dir` / `commands_dir` getters consumed by `plugins/custom_commands.lua`. |
| `lua/api/permissions.rs`                  | 205  | `smelt.permissions.{list,sync,set_rules}` — session/workspace rules + config-time rule registration | added | P3.b/P5.d | landed | `list/sync` are Pre-P5 surfaces over `RuntimeApprovals` + `store`. `set_rules` (P5.d) populates `LuaShared.permission_rules` for startup injection into `Permissions`. |
| `lua/api/process.rs`                      | 185  | `smelt.process.{list,kill,read_output,spawn_bg,run}` — long-lived registry + short-lived `core::process::run` | added | P3.b/P3.c | landed | `run_streaming` reads `current_task_cancel()` and pushes a cancel marker when the parent Lua task is aborted (P6.c). |
| `lua/api/provider.rs`                     | 87   | `smelt.provider.{register,list}` — config-time provider registration | added         | P5.d      | landed  | `register` validates `api_base`, `api_key_env`, and `models` table, then pushes a `ProviderConfig` into `LuaShared.providers`. Picked up by `Config::from_lua_shared` before engine start. `list` returns registered provider names. |
| `lua/api/prompt.rs`                       | 44   | `smelt.prompt.{win_id,text,set_text,set_section,remove_section}` | added           | P3.b      | landed  | Stable prompt-Window id + edit-buf snapshot/replace + named prompt sections.                                                  |
| `lua/api/session.rs`                      | 180  | `smelt.session.{title,cwd,cost,context_tokens,context_window,id,dir,turns,messages,rewind_to,list,load,delete,created_at_ms,fork,reset}` | added | P3.b | landed | Current-session metadata + persisted-session enumeration / load / delete; `cost` / `context_tokens` / `context_window` / `messages` carved from `smelt.engine` (P4.e). |
| `lua/api/settings.rs`                     | 72   | `smelt.settings.{snapshot,toggle,set}` — user preferences + config-time overrides | added | P3.b/P5.d | landed | `snapshot/toggle` mirror `app.settings_state()` for the `/settings` picker. `set` (P5.d) writes into `LuaShared.settings_overrides` so `Config::from_lua_shared` can apply them before engine start. |
| `lua/api/shell.rs`                        | 107  | `smelt.shell.{split,split_with_ops,check_interactive,check_background_op}` | added | P3.b      | landed  | Pure parsing helpers reused from the core bash tool; consumed by Lua wrappers (background_commands).                          |
| `lua/api/skills.rs`                       | 53   | `smelt.skills.{content,list}` over `Core.skills` (engine `SkillLoader` clone) | added               | P5.b      | landed  | Backs `runtime/lua/smelt/tools/load_skill.lua`; reads via `try_with_app` like every Host-tier binding.                       |
| `lua/api/spawn.rs`                        | 26   | `smelt.spawn` — fire-and-forget Lua coroutine                | added               | P3.b      | landed  | Routes through `LuaShared.tasks`.                                                                                            |
| `lua/api/statusline.rs`                   | 245  | `smelt.statusline.{register,unregister,snapshot}` core composer surface | added | P3.b/P4.c | landed | `register/unregister` write to `LuaShared.statusline_sources`. `snapshot()` (added P4.c) projects the full `TuiApp` state the bottom-row composer consumes (working, throbber, vim, mode, indicators, position, theme colors) into one Lua table per refresh — `runtime/lua/smelt/status.lua` reads it and returns the segment list. |
| `lua/api/syntax.rs`                       | 31   | `smelt.syntax.render(buf, { content, path })` syntect render | added               | P3.b      | landed  | Reuses `print_syntax_file` + `to_buffer::render_into_buffer`.                                                                 |
| `lua/api/task.rs`                         | 40   | `smelt.task.{alloc,resume}` external-id resolution surface   | added               | P3.b      | landed  | Drives coroutine yield primitives in `_bootstrap.lua`.                                                                       |
| `lua/api/theme.rs`                        | 72   | `smelt.theme.{accent,get,set,snapshot,is_light,presets}`     | added               | P3.b      | landed  | UiHost reads / writes through `host.ui().theme[_mut]()`; presets surface `crate::theme::PRESETS`.                             |
| `lua/api/timer.rs`                        | 61   | `smelt.timer.{set,every,cancel}` + `smelt.defer` alias       | added               | P3.b      | landed  | Schedules through `host.timers()` (Host-tier).                                                                               |
| `lua/api/tools.rs`                        | 137  | `smelt.tools.{register,unregister,resolve,__send_call}`      | added               | P3.b      | landed  | Plugin-tool registry write + engine result resolution.                                                                       |
| `lua/api/transcript.rs`                   | 24   | `smelt.transcript.{text,yank_block}` rendered transcript surface | added           | P3.b      | landed  | Joins `core.full_transcript_display_text` for plugin reads.                                                                    |
| `lua/api/ui.rs`                           | 119  | `smelt.ui.{ghost_text,spinner,picker,overlay}` overlay primitives | added           | P3.b/P4.d | partial | UiHost-only overlays; `picker._open` and the generic `overlay.open` route through `lua::ui_ops`. Dialogs compose these primitives from Lua rather than calling a Rust dialog opener. |
| `lua/api/vim.rs`                          | 11   | `smelt.vim.mode` global VimMode read                         | added               | P3.b      | landed  | Exposes `core.vim_mode` for plugins that branch on Insert / Normal.                                                            |
| `lua/api/win.rs`                          | 210  | `smelt.win.{focus,mode,close,buf,set_focus,set_keymap,on_event,clear_keymap,clear_event}` | added | P3.b | landed | UiHost-only window surface; keymap / event registration writes to `Ui` and tracks Lua handles.                                |
| `lua/app_ref.rs`                          | 122  | TLS app pointer                                             | kept                | P2.b.5    | partial | `with_host` / `try_with_host` (`pub(crate)`) + `with_ui_host` / `try_with_ui_host` (`pub`) trait-typed dispatchers shipped in P2.b.5a; `with_app` retained pending b.5c bulk migration. TLS-slot generalization (Tui-or-Headless) waits for b.5b alongside the first headless Lua driver. |
| `lua/api/confirm.rs`                      | 235  | `smelt.confirm.{_render_title,_back_tab,_resolve}` primitives + `render_title_into_buf` | moved-to-lua        | P4.d      | done    | `core/dialogs/{confirm,mod}.rs` retired; the title renderer (last Rust-side dialog primitive) inlines here as a private fn. Stays Rust-side until span-level Lua API exists. |
| `lua/mod.rs`                              | 1816 | Lua runtime                                                 | restructured        | P2.a/P3/P5.d | partial | Autocmd registry retired (events flow through Cells now); config-time APIs (`provider.register`, `mcp.register`, `permissions.set_rules`, `settings.set`) added in P5.d. |
| `lua/task.rs`                             | 653  | Single coroutine task wrapper                               | kept                | P2/P6.c   | landed  | Each `LuaTask` carries a `tokio_util::sync::CancellationToken`. `cancel_all()` flips parked `Sleep`/`External` waits to `Ready` with a `{"__cancelled"=true}` marker; `step_task` also prevents newly-yielded tasks from parking when the token is already cancelled. Thread-local `CURRENT_TASK_CANCEL` lets async Lua bindings propagate cancellation into in-flight tokio tasks. |
| `lua/tasks.rs`                            | 603  | Coroutine task registry + plugin tool env                   | restructured        | P2/P5/P6.c | landed  | Owns tool registration / hook evaluation / dispatch inside `LuaRuntime`. `cancel_tasks()` delegates to `LuaTaskRuntime::cancel_all`. `ToolRuntime` carve-out deferred (YAGNI — never materialized as a separate type). |
| `lua/ui_ops.rs`                           | 494  | Generic Lua UI helpers (overlay open/placement, picker open, reusable list/input window recipes) | restructured        | P3.b/P4.d | partial | Dialog-specific translation path removed; this file should keep shrinking toward generic helpers only. |
| `metrics.rs`                              | 538  | Frame timing                                                | kept                | P7        | pending | Review for dead code at end. Dead `stats_row_count` retired in `2e31dd0`.                                   |
| `core/notebook.rs`                             | ~850 | Jupyter notebook capability (`Notebook` parse + `apply_edit` / `render_notebook_text` / `NotebookRenderData`) | kept | P3.a/P5.b | done | Absorbed `engine/tools/notebook.rs`. `NotebookEditTool` retired; `apply_edit` and `render_notebook_text` are the Lua FFI surface. |
| `core/path.rs`                                 | 159  | Path arithmetic (normalize / canonical / relative / expand_home) | kept           | P3.a      | landed  | Pure capability; no filesystem touch outside `canonical`. Lua surface lives at `lua/api/path.rs`.           |
| `perf.rs`                                 | 286  | Profiling guards                                            | kept                | P7        | pending | Review at end                                                                                               |
| `mcp/dispatcher.rs`                       | 108  | MCP tool dispatcher + `McpToolDef` wrapper                  | added               | P5.c      | landed  | Absorbed `engine/mcp/tool_adapter.rs` into the dispatcher.                                                                    |
| `mcp/mod.rs`                              | 271  | MCP manager (`McpManager` start/stop, tool enumeration, call dispatch) | kept     | P5.c      | landed  | Moved from `engine/mcp/mod.rs`.                                                                                               |
| `core/permissions/approvals.rs`                | 234  | Runtime approval tracking                                   | kept                | P5.c      | landed  | Absorbed from `engine/permissions/approvals.rs`. `RuntimeApprovals` queried by Lua hooks.                                    |
| `core/permissions/bash.rs`                     | 515  | Bash subcommand parsing                                     | kept                | P5.c      | landed  | Absorbed from `engine/permissions/bash.rs`. FFI for `bash.lua` hook.                                                        |
| `core/permissions/mod.rs`                      | 324  | `core::permissions` capability namespace root (`Permissions` aggregate + `decide` + `load`) | kept | P3.a/P5.c | landed  | Re-exports `store`. Engine permission policy (`bash` / `rules` / `workspace` / `approvals`) absorbed here in P5.c. `load` replaced by `from_raw` (Lua-driven) in P5.d. |
| `core/permissions/rules.rs`                    | 290  | Rule matching                                               | kept                | P5.c      | landed  | Absorbed from `engine/permissions/rules.rs`. Glob compile + ruleset check.                                                  |
| `core/permissions/store.rs`                    | 202  | Workspace approval rules (JSON store: load / save / add_tool / add_dir / into_approvals) | kept | P3.a | landed  | Relocated from top-level `workspace_permissions.rs`. Lua tool hooks call via `crate::permissions::store::*` (FFI). |
| `core/permissions/tests.rs`                    | 1677 | Permission unit tests                                       | kept                | P5.c      | landed  | Absorbed from `engine/permissions/tests.rs`.                                                                                |
| `core/permissions/workspace.rs`                | 104  | Path extraction + boundary                                  | kept                | P5.c      | landed  | Absorbed from `engine/permissions/workspace.rs`. Path extract + workspace check.                                            |
| `persist.rs`                              | 153  | Session save/load                                           | merged              | P2        | done    | Into Session subsystem                                                                                      |
| `picker.rs`                               | 314  | Buffer-backed picker overlay (open / set_items / set_selected) | restructured     | P1.c/P4  | partial | P4 moves the recipe behind `widgets/picker.lua`. |
| `core/process.rs`                              | 510  | Process capability — short-lived `run`, async `run_streaming`, plus the long-lived `ProcessRegistry` (spawn / read / stop / clear / list / running_count) that optional background-process plugins/tools register against. | kept                | P3.a/P5.b/P5.c | landed  | Background-registry fold from `engine/tools/background.rs` lands this session — `kill_group_sigkill` (SIGKILL for explicit `stop_process`) sits alongside `kill_process_group` (SIGTERM for `run_streaming` timeout). Engine no longer references the registry; the foreground `Command::output` / `run_streaming` paths are unchanged. |
| `prompt_sections.rs`                      | 206  | System prompt composition                                   | kept                | P2        | pending | Engine glue                                                                                                 |
| `session.rs`                              | 587  | Session struct (metadata, costs)                            | merged              | P2.a      | done    | SoT for messages + snapshots + running cost. Final merge into Session subsystem stays P2.a.8+. |
| `sleep_inhibit.rs`                        | 140  | OS sleep suppression                                        | kept                | P2        | pending | Subsystem or utility                                                                                        |
| `state.rs`                                | 340  | App state enums + ResolvedSettings                          | restructured        | P2.a      | partial | Saved-state `State` loader + persisted-settings types stay until the AppConfig persistence rewrite. |
| `ui/theme.rs`                                | 368  | Theme bridge: populate_ui_theme + OSC11 detect + PRESETS    | narrowed            | P1.0      | landed  | Bridge fns + presets only; atomic globals collapsed onto `ui::Theme`. |
| `core/tools.rs`                                | 40   | `display_path` + `str_arg` helpers                          | added               | P5        | done    | `display_path` moved from `engine/tools/mod.rs`; `str_arg` is a JSON-args convenience used by notebook renderers. Tool summaries are defined in Lua tool metadata (`summary(args)`), with a Lua-side default installed by `_bootstrap.lua`. |
| `utils.rs`                                | 87   | General utilities                                           | kept                | none      | pending | Utility                                                                                                     |
| `window.rs`                               | 35   | Window wrapper (gutters, follow_tail)                       | merged              | P1.d      | done    | Into ui::Window                                                                                             |

## `crates/engine/src/`

| File                           | LOC  | Purpose                        | Fate         | Phase | Status  | Notes                                                           |
| ------------------------------ | ---- | ------------------------------ | ------------ | ----- | ------- | --------------------------------------------------------------- |
| `agent.rs`                     | 1949 | Agent loop, turn driver        | restructured | P5    | landed  | Drops `permissions: Permissions` field + `decide()` call; calls `ToolDispatcher::evaluate_hooks` only. Single-agent only. |
| `auth.rs`                      | 133  | Auth config + token storage    | kept         | none  | pending | Provider infra                                                  |
| `cancel.rs`                    | 53   | Cancellation token             | kept         | P6    | pending | Used by cooperative cancel                                      |
| `compact.rs`                   | 553  | Context compaction             | kept         | none  | pending | LLM-side context reduction                                      |
| `config.rs`                    | 27   | Top-level engine config        | kept         | none  | pending |                                                                 |
| `image.rs`                     | 105  | Image embedding                | kept         | none  | pending | Provider feature                                                |
| `lib.rs`                       | 247  | Engine exports + EngineHandle  | restructured | P5.a/P5.c  | landed | Tool trait slimmed; ToolDispatcher introduced; drop `EngineConfig.interactive`, `permissions`, `runtime_approvals` fields; `EngineHandle` becomes channels-only. `runtime_approvals` / `EngineConfig.permissions` dropped in P5.c. Earlier in P5: `processes: ProcessRegistry` field dropped (`ProcessRegistry` now constructed by `Core`). |
| `log.rs`                       | 120  | Trace logging                  | kept         | none  | pending |                                                                 |
| `paths.rs`                     | 151  | Workspace/config dir discovery | kept         | none  | pending |                                                                 |
| `pricing.rs`                   | 204  | Token cost calc                | kept         | none  | pending | Session/budget                                                  |
| `provider/anthropic.rs`        | 291  | Anthropic API                  | kept         | none  | pending |                                                                 |
| `provider/auth_storage.rs`     | 76   | Credential storage             | kept         | none  | pending |                                                                 |
| `provider/chat_completions.rs` | 215  | Streaming abstraction          | kept         | none  | pending |                                                                 |
| `provider/codex.rs`            | 701  | Codex provider                 | kept         | none  | pending |                                                                 |
| `provider/copilot.rs`          | 654  | Copilot provider               | kept         | none  | pending |                                                                 |
| `provider/extract.rs`          | 328  | JSON extraction                | kept         | none  | pending |                                                                 |
| `provider/mod.rs`              | 1135 | Provider trait + dispatch      | kept         | none  | pending | Core LLM boundary                                               |
| `provider/openai.rs`           | 351  | OpenAI provider                | kept         | none  | pending |                                                                 |
| `provider/sse.rs`              | 48   | SSE parsing                    | kept         | none  | pending |                                                                 |
| `redact.rs`                    | 922  | Content redaction              | kept         | none  | pending | Privacy/logging                                                 |
| `result_dedup.rs`              | 170  | Conversation result dedup      | added        | P5    | done    | Extracted from `engine/tools/result_dedup.rs`. Operates on `protocol::Message` history — an engine concern, not a tool. |
| `trim.rs`                      | 30   | Tool-output trimming for LLM context | added  | P5    | done    | Extracted from `engine/tools/mod.rs`. `MAX_TOOL_OUTPUT_LINES` + `trim_tool_output` consumed by all three provider serializers. |
| `skills.rs`                    | 219  | Skill loader                   | kept         | P5    | landed  | `pub fn content` + `pub fn names` exposed to tui via `Core.skills` so the Lua `load_skill` tool composes `smelt.skills.content`. Frontmatter parser uses manual YAML subset (no `serde_yml`) since P5.d. |
| `tools/mod.rs`                 | ~180 | Tool trait + ToolDispatcher trait + ToolResult + ctx  | restructured | P5.a  | done    | All helper functions (`str_arg`, `tool_arg_summary`, `display_path`, `confirm_with_optional_path`, `trim_tool_output`, `try_flock`, `FlockGuard`) moved out. `summary` field removed from `EngineEvent::ToolStarted`; transcript/headless labels now come from Lua tool metadata. Engine surface is now strictly `ToolResult`, `ToolContext`, `ToolFuture`, `Tool`, `ToolEntry`, `ToolDispatcher`, `ToolRegistry`, `build_tools()`. |

## `crates/protocol/src/`

| File         | LOC | Purpose                                 | Fate         | Phase | Status  | Notes                                                                                                |
| ------------ | --- | --------------------------------------- | ------------ | ----- | ------- | ---------------------------------------------------------------------------------------------------- |
| `content.rs` | 171 | Content / ContentPart types             | kept         | none  | pending | Wire types                                                                                           |
| `event.rs`   | 414 | EngineEvent + UiCommand                 | restructured | P5.c/P5.e | partial | P5.e rename pass landed (`SetMode`→`SetAgentMode`, `ExecutePluginTool`→`ToolDispatch`, `EvaluatePluginToolHooks`→`ToolHooksRequest`, `PluginToolResult`→`ToolResult`, `PluginToolHooksResult`→`ToolHooksResponse`, `PluginToolDef/HookFlags/Hooks`→`ToolDef/HookFlags/Hooks`). P5.c: drop `EngineEvent::{AgentMessage, AgentExited, Spawned}`, `UiCommand::AgentMessage`, and `AgentBlockData`. |
| `lib.rs`     | 26  | Re-exports                              | kept         | none  | pending |                                                                                                      |
| `message.rs` | 200 | Message / Role / ToolCall / ToolOutcome | restructured | P5.c  | landed  | `Role::Agent` deleted in P5.c. |
| `mode.rs`    | 116 | `AgentMode` enum + ReasoningEffort      | renamed      | P5.e  | landed  | `Mode` → `AgentMode` rename pass landed; protocol export + every consumer updated.                  |
| `usage.rs`   | 102 | TokenUsage / TurnMeta / overrides       | kept         | none  | pending | Wire types                                                                                           |

## `runtime/lua/smelt/`

| File                              | LOC | Purpose                               | Fate         | Phase | Status  | Notes                                                              |
| --------------------------------- | --- | ------------------------------------- | ------------ | ----- | ------- | ------------------------------------------------------------------ |
| `_bootstrap.lua`                  | 135 | Yield primitives, fuzzy ranking       | kept         | P6.c  | landed  | `smelt.sleep`, `smelt.task.wait`, and `smelt.tools.call` check for `__cancelled` in the resumed value and call `error("cancelled")` so Lua `pcall` can catch it. |
| `cmd.lua`                         | 82  | Command framework wrappers            | kept         | none  | pending | Stays as the top-level framework helper that plugin-defined commands call (sibling to `dialog.lua`). The `commands.lua` umbrella idea retires. |
| `dialogs/confirm.lua`             | 200 | Confirm dialog renderer               | moved        | P4.a/P4.d | landed  | Composes generic `buf` / `win` / `overlay` primitives via `smelt.ui.dialog.open_handle` (defined in Lua); reads request payload from the `confirm_requested` cell; reason via panel handle's `:text()`; selection via `selection_changed`; typed-reason via `text_changed`. |
| `dialog.lua`                      | 269 | Dialog handle factory + framework     | kept         | P4.a  | pending | Primary dialog abstraction. C.9b: panel handles capture leaf WinId from `named_inputs`; `kind = "input"` panels expose `:text()`; `:focus()` routes to `smelt.win.set_focus` for input panels. |
| `widgets/picker.lua`              | 68  | Generic picker helper                 | moved        | P4.a  | landed  | Bootstrap chunk; `smelt.ui.picker.open` recipe.                    |
| `widgets/prompt_picker.lua`       | 164 | Prompt-docked picker                  | moved        | P4.a  | landed  | Bootstrap chunk; `smelt.prompt.open_picker`. Stays separate from `widgets/picker.lua` per P4.a decision (different focus / filtering shapes). |
| `dialogs/agents.lua`              | 248 | `/agents` dialog                      | moved        | P4.a/P4.d | landed  | Module path `smelt.dialogs.agents`; orchestrates via `smelt.ui.dialog`.                                          |
| `tools/ask_user_question.lua`     | 119 | Built-in `ask_user_question` tool     | moved        | P5.b  | landed  | Already-Lua tool relocated from `plugins/`; lives under `tools/` so the directory matches the migration target. Embedded module path is `smelt.tools.ask_user_question`. |
| `plugins/background_commands.lua` | 165 | `read_process_output` + `stop_process` tools + `/ps` slash-command | restructured | P5.b/P5.c | partial | Target shape is the background-process plugin surface: registry-oriented tools and `/ps` stay here, and any background execution entry point should live here (or an equivalent optional plugin), not in `tools/bash.lua`. |
| `plugins/btw.lua`                 | 58  | `/btw` side-question                  | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/color.lua`               | 30  | `/color` slug accent                  | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/export.lua`              | 159 | `/export` markdown                    | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/help.lua`                | 47  | `/help` keybinding viewer             | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/history_search.lua`      | 50  | Ctrl+R history search                 | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/model.lua`               | 37  | `/model` picker                       | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `dialogs/permissions.lua`         | 97  | `/permissions` dialog                 | moved        | P4.a/P4.d | landed  | Module path `smelt.dialogs.permissions`; orchestrates via `smelt.ui.dialog`.                                  |
| `plugins/plan_mode.lua`           | 68  | Plan-mode hook driver                 | kept         | P5.b  | landed  | Mode-hook surface only: swaps the plan-mode prompt section + the `exit_plan_mode` tool in / out as the agent enters / leaves Plan mode. Tool body lives in `smelt.tools.exit_plan_mode`. |
| `plugins/compact.lua`             | 5   | `/compact [instructions]`             | added        | P4.e  | landed | Migrated `cmd_compact` (`abe9ed0`); wraps `smelt.engine.compact`. Declares `while_busy = false` (`68f8317`).         |
| `plugins/reflect.lua`             | 56  | `/reflect [focus]`                    | added        | P4.e  | landed | Self-contained: prompt body inlined as a Lua string; submits via `smelt.engine.submit_command("reflect", body)`.   |
| `plugins/simplify.lua`            | 159 | `/simplify [focus]`                   | added        | P4.e  | landed | Self-contained: prompt body inlined as a Lua string; submits via `smelt.engine.submit_command("simplify", body)`. |
| `plugins/custom_commands.lua`     | 175 | User-defined `/foo` from `~/.config/smelt/commands/*.md` | added | P4.e | landed | Scans `smelt.path.commands_dir()` at startup, registers one slash command per `.md` file. Handler re-reads the file on every call so user edits take effect live; YAML frontmatter parses via `smelt.parse.frontmatter`; exec blocks (`!`-fenced + `!`backtick`) evaluate in pure Lua via `smelt.process.run`; submits via `smelt.engine.submit_command(name, body, overrides)`. Registers with `queue_when_busy = true` so mid-turn invocations defer cleanly. |
| `plugins/predict.lua`             | 71  | Input prediction                      | kept         | none  | pending | Stays in `plugins/` — adding a `hooks/` dir for one file is bookkeeping without payoff. |
| `plugins/quit.lua`                | 13  | `/exit`, `/quit`, `/q`, `/qa`, `/wq`, `/wqa` | added | P4.e | landed | Migrated `cmd_quit` (`7c028d8`); calls top-level `smelt.quit()` which flips `pending_quit`. |
| `dialogs/resume.lua`              | 172 | `/resume` session picker              | moved        | P4.a/P4.d | landed  | Module path `smelt.dialogs.resume`; orchestrates via `smelt.ui.dialog` + `smelt.ui.picker`.                       |
| `dialogs/rewind.lua`              | 57  | `/rewind` turn picker                 | moved        | P4.a/P4.d | landed  | Module path `smelt.dialogs.rewind`; orchestrates via `smelt.ui.picker`.                                           |
| `plugins/session.lua`             | 16  | `/clear`, `/new`, `/fork`, `/branch`  | added        | P4.e   | landed | Migrated `cmd_clear` / `cmd_fork` (`04f6419`); composes `smelt.session.{fork,reset}`.                                            |
| `plugins/settings.lua`            | 38  | `/settings` toggles                   | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/stats.lua`               | 26  | `/stats`, `/cost` dialog commands     | added        | P4.d/e | landed | Migrated `cmd_stats` / `cmd_cost` (`11bd6c6`) — first builtins to leave `RUST_COMMANDS`. Composes `smelt.ui.dialog.open` over `smelt.metrics.*`. |
| `plugins/theme.lua`               | 32  | `/theme` accent picker                | kept         | P4    | pending | Stays one-file-per-command in `plugins/`; plugin-authored colorschemes still ride `colorschemes/<name>.lua`. |
| `plugins/toggles.lua`             | 13  | `/vim`, `/thinking` toggles           | kept         | none  | pending | Stays one-file-per-command in `plugins/`.                          |
| `plugins/yank_block.lua`          | 12  | Optional `/yank-block`                | kept         | P4    | pending | Stays as opt-in plugin                                             |
| `colorschemes/default.lua`        | 9   | Default colorscheme (ember accent)    | added        | P4.a  | landed  | Seed (`9bb2f11`); `require`-able as `smelt.colorschemes.default`. Pattern target for plugin-authored schemes. |
| `status.lua`                      | 170 | Bottom-row statusline composer        | added        | P4.c  | landed  | Bootstrap chunk; registers a `core` source via `smelt.statusline.register` whose handler walks `smelt.statusline.snapshot()` and emits the full segment list (slug pill, vim mode, agent mode, throbber sub-spans, permission/proc/agent indicators, right-aligned position). The Rust `core/status_bar.rs` collapses to a buffer-write shell. |
| `modes.lua`                       | 32  | Lua-side cycle for mode + reasoning   | added        | P4.f  | landed  | Bootstrap chunk; overrides `smelt.{mode,reasoning}.cycle` over new `cycle_list()` bindings. Rust `cycle_within` / `toggle_mode` / `cycle_reasoning` retire. |
| `tools/glob.lua`                  | 53  | Built-in `glob` tool                  | added        | P5.b  | landed  | First core-tool migration off engine. `override = true` plugin tool composes `smelt.fs.glob` (globset + ignore::WalkBuilder) and inherits the `needs_confirm` shape from the retired Rust `GlobTool`. |
| `tools/grep.lua`                  | 175 | Built-in `grep` tool                  | added        | P5.b  | landed  | `override = true` plugin tool over `smelt.grep.run` (ripgrep) with a `smelt.process.run("grep", ...)` fallback. Lua-side `offset` / `head_limit` slicing + stdout+stderr concatenation match the retired Rust `GrepTool`. Schema preserves the `-i` / `-n` / `-A` / `-B` / `-C` / `context` / `multiline` / `output_mode` knobs verbatim. |
| `tools/load_skill.lua`            | 33  | Built-in `load_skill` tool            | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.skills.content` (FFI into the shared `engine::SkillLoader` parked on `Core.skills`). Engine still consumes `prompt_section()` for the system prompt; the `LoadSkillTool` Rust impl retires alongside. |
| `tools/list_agents.lua`           | —   | `list_agents` tool                    | deleted      | P5.c  | landed  | Deleted in P5.c; future multi-agent plugin may reintroduce over `smelt.process` long-lived IPC. |
| `tools/stop_agent.lua`            | —   | `stop_agent` tool                     | deleted      | P5.c  | landed  | Deleted in P5.c. |
| `tools/peek_agent.lua`            | —   | `peek_agent` tool                     | deleted      | P5.c  | landed  | Deleted in P5.c. |
| `tools/message_agent.lua`         | —   | `message_agent` tool                  | deleted      | P5.c  | landed  | Deleted in P5.c. |
| `tools/web_search.lua`            | 87  | Built-in `web_search` tool            | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.http.{post,cache.{get,put},random_user_agent}` + `smelt.html.parse_ddg_results`. Mirrors the retired Rust `WebSearchTool`: 15-minute cache keyed `search:<query>`, rotated User-Agent against DuckDuckGo's HTML endpoint, top-20 results formatted as a numbered list. |
| `tools/write_file.lua`            | 90  | Built-in `write_file` tool            | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.fs.{exists,mkdir_all,write,try_flock,file_state.{has,staleness_error,record_write}}` + `smelt.notebook.is_notebook_path` + `smelt.path.display`. Mirrors the retired `WriteFileTool`: rejects clobbering an unread file (`UNREAD_OVERWRITE_ERR`), surfaces stale-mtime preflight, locks the path before overwriting, refuses to write to `.ipynb` (deferred to `edit_notebook`). |
| `tools/edit_file.lua`             | 153 | Built-in `edit_file` tool             | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.fs.{read,write,try_flock,file_state.{staleness_error,record_write}}` + `smelt.notebook.is_notebook_path` + `smelt.path.display`. Lua-side `find/replace_first/replace_all` over `string.find(..., true)` reproduces the byte-exact replacement of the retired `EditFileTool`: rejects when `old == new`, when `old_string` is missing, when ambiguous without `replace_all = true`. |
| `tools/read_file.lua`             | 130 | Built-in `read_file` tool             | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.image.{is_image_file,read_as_data_url}` (image data-URL short-circuit) + `smelt.notebook.{is_notebook_path,read}` (line-numbered notebook formatting via the new public `engine::tools::notebook::render_notebook_text` wrapper) + `smelt.fs.{read,file_state.{get,record_read,mtime_ms}}`. Pure-Lua `effective_range` / `dedup_stub` / `format_text_window` reproduce the retired `ReadFileTool` semantics: `FILE_UNCHANGED_STUB` on exact-range repeat against unchanged mtime, default `(1, 2000)` window, 2000-byte per-line truncation, `"%4d\t%s"` line numbering. |
| `tools/notebook_edit.lua`         | 87  | Built-in `edit_notebook` tool         | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.fs.{exists,try_flock,file_state.staleness_error}` + `smelt.path.display` + a thin call into the new `smelt.notebook.apply_edit(args)` FFI (over `engine::tools::notebook::apply_edit`). The intricate JSON cell-munging stays Rust; Lua owns schema, hook, flock acquisition, and metadata pass-through (the dialog-preview payload comes back as a Lua table set on the tool result's `metadata`). Side-effect of the migration: `ToolContext.file_locks` + `engine::tools::FileLocks` retire (no remaining tool needs intra-process per-path serialization on top of `try_flock`). |
| `tools/web_fetch.lua`             | 248 | Built-in `web_fetch` tool             | added        | P5.b  | landed  | `override = true` plugin tool composing `smelt.http.{get,cache.{get,put},random_user_agent}` + `smelt.html.{title,links,to_text,to_markdown}` + `smelt.image.data_url_from_bytes` + `smelt.engine.ask` (parked on `smelt.task.alloc/wait` until `EngineAskResponse` fires). Mirrors the retired Rust `WebFetchTool`: domain-pattern approval suggestion, cf-mitigated 403 retry with `User-Agent: smelt`, cross-domain redirect rejection, image content-type → base64 data URL, 2000-line / 50KB output truncation, 15-minute cache keyed `fetch:<url>:<format>`. LLM extraction routes through `AuxiliaryTask::Btw` (was the primary model unconditionally before). |
| `tools/exit_plan_mode.lua`        | 127 | Built-in `exit_plan_mode` tool        | added        | P5.b  | landed  | Module exposing `register / unregister` so `plugins/plan_mode.lua` can swap the tool in and out as the agent enters / leaves Plan mode. Tool body — schema, plan-name generator (adjective/noun/verb tuple), `save_plan` writer under `<session>/plans/<name>.md`, confirm dialog with auto-apply / approve / deny — relocated verbatim from `plan_mode.lua`. |
| `tools/bash.lua`                  | 95  | Built-in `bash` tool                  | restructured | P5.b/P5.c | partial | `override = true` plugin tool composing `smelt.shell.{check_interactive,check_background_op,split,is_default_bash_allow}` + `smelt.process.run_streaming` for foreground shell execution. Target shape keeps `bash` foreground-only; background execution moves to a separate plugin/tool surface. |
| `tools/spawn_agent.lua`           | —   | `spawn_agent` tool                    | deleted      | P5.c  | landed  | Deleted in P5.c. |

**To be created (P4.a):**

- `widgets/{input,options,list,cmdline,statusline,notification}.lua`
- `transcript.lua`, `diff.lua`, `commands.lua`

## `src/`

| File         | LOC | Purpose                               | Fate | Phase | Status  | Notes                                 |
| ------------ | --- | ------------------------------------- | ---- | ----- | ------- | ------------------------------------- |
| `main.rs`    | 566 | CLI entry, args, dispatch             | kept | P2/P5.d | landed  | Builds `LuaRuntime` before engine start for config registration; `--config` points to custom `init.lua`. |
| `setup.rs`   | 322 | Interactive setup flows               | kept | P5.d  | landed  | First-run wizard writes Lua `init.lua` seed; `smelt auth` for non-OAuth prints Lua block for manual paste. |
| `startup.rs` | 376 | Resolve config/keys/models pre-engine | kept | P2/P5.d | landed  | Reads Lua-populated config from `LuaRuntime::to_config()`; produces `ResolvedStartup` for App init. |

## To be created (Rust capability modules, P3.a)

Target namespace `core::<name>`, currently live as `crates/tui/src/<name>.rs` (or small folder if needed):

| Module          | Purpose                           | Composes from today's                                                   |
| --------------- | --------------------------------- | ----------------------------------------------------------------------- |
| `core::parse`    | markdown / diff / syntax          | `term/content/highlight/{inline,diff,syntax,util}` + `content/stream_parser` |
| `core::process`  | (landed: ProcessRegistry + run + run_streaming) | absorbed `engine/tools/background.rs` this session (registry shipped via `Core.processes`); engine no longer references either side |
| `core::process` (long-lived IPC) | spawn · send · on_event · wait · kill | `engine/socket.rs` + `engine/registry.rs` (both deleted in P5.c); deferred — surface covered by `smelt.process.run` / `run_streaming` today |
| `core::http`     | fetch / cache / redirects         | `engine/tools/web_cache` (legacy TTL cache for pricing only after `web_fetch` migration) |
| `core::html`     | html → markdown                   | landed (`Markdown { title, content, links }` from the retired `engine/tools/web_shared.rs`) |
| `core::notebook` | Jupyter JSON ops                  | extracted from `engine/tools/notebook`                                  |
| `core::grep`     | ripgrep wrapper                   | `engine/tools/grep`                                                     |
| `core::permissions` | all permission policy: bash AST · pattern match · workspace check · runtime approvals · workspace JSON store | folds entire `engine/permissions/{approvals,bash,rules,workspace,tests}.rs` (the `workspace JSON store` half landed `e34da06` as `core::permissions::store`) |

`core::fuzzy` (landed `b537d1a`), `core::path` (landed `de7fb87`),
`core::fs` (landed `5de3054`), and `core::permissions::store` (landed
`e34da06`, namespace shell) live in the main `crates/tui/src/` table
above. `core::fs` shipped as sync std::fs primitives only; the engine
tool absorption (`file_state`, `read_file`, `write_file`, `edit_file`,
`glob`) waits on those tools migrating to Lua in P5.b. `core::permissions`
shipped as a namespace seeded with the workspace JSON store; the engine
permission policy (`bash` / `rules` / `workspace` / `approvals` + the
1617-line test suite) lands here in P5.c when engine becomes policy-free.
