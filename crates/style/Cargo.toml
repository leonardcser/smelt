[package]
name = "smelt-style"
version = "0.5.0-alpha.1"
edition = "2021"
description = "Frontend-neutral style primitives — Color, Style, Theme, HlGroup. Shared leaf consumed by document models (smelt-buffer) and renderers (smelt-term)."
license = "MIT"
repository = "https://github.com/leonardcser/smelt"

[lib]
name = "smelt_style"
path = "src/lib.rs"
